\documentclass[conference]{IEEEtran}
\IEEEoverridecommandlockouts

\usepackage{amsmath,amssymb,amsfonts}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{url}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{placeins}
\usepackage{cite}
\usepackage{subcaption}
\usepackage[colorlinks=true,linkcolor=blue,citecolor=blue,urlcolor=blue,bookmarksnumbered]{hyperref}

\graphicspath{{figures/}{./}}
\setlength{\abovecaptionskip}{18pt plus 2pt minus 2pt}
\setlength{\belowcaptionskip}{10pt plus 1pt minus 1pt}
\setlength{\textfloatsep}{14pt plus 2pt minus 2pt}

% ---- syntax-highlighted Python listing style (pdflatex-safe, no minted) ----
\definecolor{codegreen}{rgb}{0.00,0.55,0.20}
\definecolor{codegray}{rgb}{0.50,0.50,0.50}
\definecolor{codepurple}{rgb}{0.58,0.00,0.82}
\definecolor{codeblue}{rgb}{0.10,0.20,0.80}
\definecolor{backcolour}{rgb}{0.97,0.97,0.97}
\lstdefinestyle{py}{
  language=Python, backgroundcolor=\color{backcolour},
  commentstyle=\itshape\color{codegreen}, keywordstyle=\bfseries\color{codeblue},
  stringstyle=\color{codepurple}, numberstyle=\tiny\color{codegray},
  basicstyle=\ttfamily\scriptsize, breaklines=true, captionpos=b,
  keepspaces=true, numbers=left, numbersep=5pt, showstringspaces=false,
  frame=single, framesep=3pt, tabsize=2, columns=fullflexible,
  literate={->}{{$\rightarrow$}}1}
\lstset{style=py}

\begin{document}

\title{An AI-Ready Air-to-Ground Channel Model for HAPS--UAV Access Networks\\
\large Channel-Model Implementation and Signal-Quality Characterisation}

\author{\IEEEauthorblockN{Harsha Matadhikari}
\IEEEauthorblockA{IIT Delhi Online PG Diploma Advanced Comm Engg with Quantum AI Integration}}

\maketitle

\begin{abstract}

This paper presents a simulation and validation
study for an AI-driven HAPS–UAV–Ground access network,
addressing challenges like SINR heterogeneity induced by a fixed
3-HAPS equilateral-triangle constellation across its service foot-
print. Following Arani et al., we implement (i) a free-space model
for the High-Altitude Platform Station (HAPS) link, (ii)
building-blockage line-of-sight (LoS) probability series for the
Unmanned-Aerial-Vehicle (UAV) links, (iii) average
path loss and downlink signal-to-interference-plus-noise ratio
(SINR) under this constellation geometry, and (iv) comprehensive
ITU-R atmospheric losses (rain, fog, gas, scintillation) for feeder-
link design. The channel model is realised in Python (NumPy +
Matplotlib + itur package) and characterised through signal-
quality curves, spatial-heterogeneity analysis, and atmospheric
sensitivity results.
\end{abstract}

\begin{IEEEkeywords}
HAPS, UAV, non-terrestrial networks, air-to-ground channel, LoS probability, SINR, multi-node networks, UAV relay, feeder link, beam tracking, rain attenuation, system geometry
\end{IEEEkeywords}

% =====================================================================
\section{Introduction}
% =====================================================================

\subsection{Platform Overview}

In this HAPS-anchored access network, a fixed HAPS and a fleet of UAVs jointly serve mobile ground users. The control problem is to decide each UAV’s position and channel assignment so that users are served fairly and reliably. Since the reward is computed from user data rates, which depend on the underlying propagation geometry, a simple distance-based heuristic is insufficient. This motivates a DRL-based controller, which learns the joint placement and channel-selection policy directly from the network state.

A typical HAPS system consists of a stratospheric, solar-powered platform operating at approximately 20 km altitude. At this altitude, it maintains a fixed service footprint while undergoing continuous circular flight and attitude variations (roll, pitch, and yaw), supporting a service radius of up to 100 km~\cite{itu_m2101,itu_m2106}. It communicates with ground equipment through

\begin{itemize}
  \item {Feeder-link: HAPS to Gateway, f = 38 -- 39.5 GHz (Ka-band)~\cite{itu_m2101}} \\
   Feeder Links operate in the 38.0–39.5 GHz millimeter-wave band allocated to HAPS by ITU-R M.2101. This link provides high-capacity backhaul between the Ground Gateway (GW) and the aircraft, standardized by 3GPP NTN~\cite{3gpp_ts38821}.

  \item { Service link:  HAPS to User, f = 2.1 -- 2.2 GHz (S-band)~\cite{3gpp_ts38821}} \\
 Service links operate in the 2 GHz FDD band (3GPP Band 1/3). This link is designed for connectivity to  user equipment, ensuring accessibility without
 specialized hardware, a critical factor for disaster recovery scenarios.

  \item {Relay Link: User to UAV to HAPS, f = 2.1 -- 2.2 GHz~\cite{itu_m2101}} \\ Relay links the ground UEs with the stratospheric nodes. They operate in the 2 GHz S-band, fog resistant~\cite{itu_p618,itu_p838}, sustain connectivity over a low-altitude moisture layer
  where higher frequencies face degradation~\cite{itu_p676}. It serves as the primary backup path for users when direct-to-HAPS service links are obstructed by buildings or heavy atmospheric moisture.

\end{itemize}

\subsection{Atmospheric Impacts on Connectivity}

\begin{itemize}
  \item Frequency-Specific Impact: The 38.0--39.5 GHz feeder link (Ka-band) incurs significant atmospheric loss from rain (~8--18 dB at 10--25 mm/h per ITU-R P.838~\cite{itu_p838}), fog~\cite{itu_p840}, and gas absorption~\cite{itu_p676}. Although lower frequencies (10--20 GHz) experience less rain attenuation, the 38 GHz band is chosen as a compromise: it avoids the 22.235 GHz water-vapor absorption resonance~\cite{itu_p676} and the catastrophic 60 GHz oxygen peak~\cite{itu_p676}, while offering practical spectrum allocation and manageable link margins. In contrast, the 2 GHz service link exhibits high resilience to atmospheric effects according to ITU-R P.618~\cite{itu_p618}, providing a stable ``last-mile'' connection even under adverse weather.
  \item Path Length and Elevation: Because the HAPS provides coverage up to a radius of 100 km~\cite{itu_m2106}, many links operate at low elevation angles. These "slant paths" (§2.1, worst-case 102 km) result in a significantly longer signal trajectory through the moisture-dense troposphere compared to vertical (nadir) paths, leading to higher cumulative attenuation per ITU-R P.618~\cite{itu_p618}.
\end{itemize}

% =====================================================================
\section{System Geometry and Link Types}
% =====================================================================

\subsection{Network Topology}

\begin{enumerate}
  \item \textbf{Access layer}: 3 HAPS nodes + 1 UAV relay serve ground users.
  \item \textbf{Backhaul layer}: 1 ground gateway aggregates feeder links from all 3 HAPS.
  \item \textbf{Core network}: Gateway connects to Internet.
\end{enumerate}

\begin{table}[t]
\centering
\caption{POC Network Configuration (AGL = Above Ground Level)}
\vspace{2mm}
\label{tab:config}
{\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}X|>{\raggedright\arraybackslash}X|}
\hline
\textbf{Node} & \textbf{Altitude} & \textbf{Count} & \textbf{Spacing} \\
\hline
HAPS & 20\,km & 3 & Equilateral 65km, 1 Server, 2 Interferer \\
\hline
UAV Relay & 500\,m AGL & 1 & Static (no mobility) \\
\hline
Gateway & 50\,m AGL & 1 & Centroid of HAPS triangle \\
\hline
Users & 1.5\,m (hand-held) & $N$ (configurable) & Uniform random in coverage \\
\hline
\end{tabularx}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=0.9\linewidth]{./figures/network_diagram.pdf}
\vspace{3mm}
\caption{Topology}
\label{fig:simulation-topology}
\end{figure}
\FloatBarrier

\noindent The UAV Relay node's geometry is configured as above, but its SINR performance
is not analyzed in this document; the channel model in \S III--IV covers only the direct
HAPS service and feeder links, with the two-hop User--UAV--HAPS relay path left unmodeled.


\subsection{Feeder Link Geometry}

The feeder link (HAPS-to-Gateway, 38--39.5 GHz) is a short-range backhaul connection from HAPS
aircraft to the ground-based Gateway. Unlike the service link (which spans the full 100 km service
radius), the feeder link operates over distances of only 10--500 m. For HAPS at 20 km altitude and
Gateway at 50 m AGL, at nominal 100 m horizontal distance:

\begin{itemize}
  \item Horizontal distance (typical): 10--500 m (Gateway positioning near HAPS nadir)
  \item Altitude difference: 20 km (HAPS) - 0.05 km (Gateway) $\approx$ 20 km
  \item Elevation angle: $\theta = \arctan(20 / 0.1) \approx 89.7^\circ$ (nearly zenith)
  \item Slant path: $L_{\text{slant}} = \sqrt{0.1^2 + 20^2} \approx 20$ km
\end{itemize}

The high elevation angle ($\approx 90^\circ$, near-vertical link) significantly reduces atmospheric
path loss compared to the low-elevation service link. The short slant path ($\approx 20$ km) through
the troposphere results in manageable rain attenuation and scintillation, making the feeder link
primarily noise-limited at Ka-band (as shown in \S~\ref{sec:feeder-sinr}).

% =====================================================================
\section{System and Channel Model}
% =====================================================================

This section presents three interrelated channel models: (A) generic propagation formulae applicable to all frequency bands, (B) the 38 GHz feeder link model (HAPS-to-Gateway backhaul with atmospheric losses), and (C) the 2 GHz service/relay link model (UAV-to-user air-to-ground with building blockage).

% =====================================================================
\subsection{III.A: Common System Models (All Frequency Bands)}
% =====================================================================

\subsubsection{Free-space path loss}
FSPL is calculated on the basis of a 20 km vertical zenith distance and a lateral reach extending to 100 km.
The ITU-R P.530~\cite{itu_p530} free-space path loss formula FSPL(MHz, km) is defined as
\begin{equation}
L_\mathrm{FSPL} = 32.44 + 20\log_{10} f_\mathrm{MHz} + 20\log_{10} d_\mathrm{km} \;[\mathrm{dB}].
\label{eq:fspl}
\end{equation}
\noindent\textbf{Equation (1) --- Free-Space Path Loss~\cite{itu_p530}}: $f_\mathrm{MHz}$ is the frequency of the HAPS carrier in MHz and $d_\mathrm{km}$ is the distance in km. This formula applies for all frequencies and distances in the HAPS service footprint, establishing the baseline attenuation before atmospheric and propagation effects. Used in both feeder-link (38 GHz, §III.B) and service-link (2 GHz, §III.C) budgets.

% =====================================================================
\subsection{III.B: 38 GHz Feeder Link Model (HAPS-to-Gateway Backhaul)}
% =====================================================================

The feeder link (HAPS to Ground Gateway, 38--39.5 GHz Ka-band per ITU-R M.2101) operates over worst-case slant paths up to 102 km at low elevation angles (11.3$^\circ$ worst-case, determined by the 100 km service-radius edge). This path is vulnerable to atmospheric attenuation from rain, fog, gas absorption, and scintillation. Section III.B models each component sequentially.

\paragraph{Rain attenuation (ITU-R P.618~\cite{itu_p618}, P.838~\cite{itu_p838})}
Rain attenuation $\gamma_R$ (dB/km) is calculated from the local rain
rate $R$ (mm/h) using the empirical ITU-R P.838 coefficients~\cite{itu_p838} $k,\alpha$:
\begin{equation}
\gamma_R = k(f,\theta) \; R^{\alpha(f,\theta)} \quad [\mathrm{dB/km}],
\label{eq:rain_spec_atten}
\end{equation}

\noindent\textbf{Equation (2) --- Specific Rain Attenuation~\cite{itu_p838}}: For 38 GHz, typical coefficients are $k=0.0318$ and $\alpha=0.921$ per ITU-R P.838 Annex 1~\cite{itu_p838}. These values are interpolated from Table 1 (P.838 Annex 1) and represent the power-law relationship between the rain rate and attenuation.

When we multiply the effective path length $L_\mathrm{eff}$ (km) by this, we
obtain the rain loss:
\begin{equation}
A_\mathrm{rain} = \gamma_R \; L_\mathrm{eff} \quad [\mathrm{dB}],
\label{eq:rain_loss}
\end{equation}

\noindent\textbf{Equation (3) --- Total Rain Attenuation~\cite{itu_p618}}: $L_\mathrm{eff}$ is computed according to ITU-R P.618~\cite{itu_p618} (slant-path reduction factors and
site-dependent scaling for non-uniform rain along the path). For the worst-case feeder link (102 km slant, 11.3$^\circ$ elevation),
$L_\mathrm{eff} \approx 66$ km with reduction factor $r(\theta) \approx 0.65$, which yields an attenuation of approximately \textbf{5.9 dB at 10 mm/h} and \textbf{15.2 dB at 25 mm / h}~\cite{itu_p618}.


\paragraph{Clouds and fog (ITU-R P.840~\cite{itu_p840})}
Fog attenuation depends on the liquid-water density $M$ [g/m$^3$], frequency
$f$, and temperature $T$ per ITU-R P.840~\cite{itu_p840}. It is negligible below about 10 GHz, becomes noticeable
in the millimeter-wave bands, and can be several dB in dense fog.
Typical values are approximately 0.05 g/m$^3$ for medium fog and 0.5 g/m$^3$ for
dense fog~\cite{itu_p840}.
Cloud/fog attenuation is modeled as a specific attenuation $\gamma_c$ (dB/km)
times the slant-path length $L_\mathrm{slant}$ (km):
\begin{equation}
A_\mathrm{cloud} = \gamma_c(f,M,T) \;L_\mathrm{slant} \quad [\mathrm{dB}],
\label{eq:fog_loss}
\end{equation}

\noindent\textbf{Equation (4) --- Fog/Cloud Attenuation~\cite{itu_p840}}: Specific attenuation $\gamma_c$ depends on frequency and liquid-water-content per ITU-R P.840~\cite{itu_p840}.
where $\gamma_c$ depends on the liquid-water density $M$ [g/m$^3$], frequency
and, weakly, on temperature. Typical liquid-water densities are $M\approx0.05$ for
medium fog and $M\approx0.5$ for dense fog; $\gamma_c$ is small below 10 GHz and
grows into the millimeter-wave bands~\cite{itu_p840}. For the 38 GHz feeder link,
fog contributes negligibly to total loss (typically $<$0.2 dB).


\paragraph{Atmospheric gases (ITU-R P.676~\cite{itu_p676})}
Atmospheric gas attenuation combines oxygen and water-vapour specific
attenuations $\gamma_o$ and $\gamma_w$ (dB/km) computed from 
local pressure $p$, temperature $T$ and water-vapour density $\rho_w$ according to ITU-R P.676~\cite{itu_p676}.
The total gas loss is
\begin{equation}
A_\mathrm{gas} = (\gamma_o(p,T,f) + \gamma_w(\rho_w,T,f))\;L_\mathrm{slant} \quad [\mathrm{dB}],
\label{eq:gas_loss}
\end{equation}

\noindent\textbf{Equation (5) --- Atmospheric Gas Absorption~\cite{itu_p676}}: Combines oxygen and water-vapor components according to ITU-R P.676 Sections 3.1 and 3.2~\cite{itu_p676}. The strongest absorption lines occur at 22.235 GHz and 183 GHz for water
vapor, and near 59.8 GHz and 118.75 GHz for oxygen~\cite{itu_p676} (P.676 Sect. 3.1--3.2, Figure 2).
This makes Ka/Q/V-band HAPS feeder bands much more sensitive to moisture than the sub-6 GHz access links.
At 38 GHz, clear-sky gas absorption is approximately 0.2--0.3 dB/km, orders of magnitude lower than
at the 60 GHz oxygen resonance ($\sim$15--20 dB/km), justifying the 38 GHz band choice per ITU-R analysis~\cite{itu_p676}.

\subsection{Tropospheric scintillation (ITU-R P.618\texorpdfstring{\cite{itu_p618}}{[ITU-R P.618]})}
Tropospheric scintillation refers to rapid fluctuations in signal amplitude and
phase caused by small-scale refractive-index irregularities in the lower
troposphere per ITU-R P.618~\cite{itu_p618}. In Ka-band (around 38 GHz) feeder links, scintillation can cause
dynamic fading even in clear-sky conditions and must be taken into account when
designing beam-tracking and link-budget margins~\cite{itu_p618}.

The fade depth due to scintillation for a specific time percentage $p$ is
commonly expressed as short-term amplitude fluctuations using the time percentage model (ITU-R P.618, Annex 1~\cite{itu_p618}):
\begin{equation}
A_\mathrm{scint}(p) = a(p) \cdot \sigma_{pre}(f,\theta,\mathrm{climate}) \quad [\mathrm{dB}],
\label{eq:scint_loss}
\end{equation}

\noindent\textbf{Equation (6) --- Scintillation Fade Depth~\cite{itu_p618}}: where $a(p)$ maps standard-deviation values to time-percentage fades (ITU-R P.618 Annex 1~\cite{itu_p618})
and $\sigma_{pre}$ is calculated from the frequency, elevation angle $\theta$ and
local turbulence parameters in the recommendation. We then scale by $a(p)$ to obtain fade depths
for availability analyses and margin setting in adaptive systems. For 38 GHz at 11.3$^\circ$ elevation
and 0.01% outage, typical scintillation fade is 2--4 dB~\cite{itu_p618}.

So total slant-path attenuation for a feeder link may be written as
\begin{equation}
L_\mathrm{total} = L_\mathrm{FSPL} + A_\mathrm{rain} + A_\mathrm{cloud/fog}
                  + A_\mathrm{gas} + A_\mathrm{scintillation},
\label{eq:feeder_loss}
\end{equation}

\noindent\textbf{Equation (7) --- Total Feeder Link Attenuation~\cite{itu_p618}}: This equation sums all atmospheric and propagation effects (Eqs. 1--6) to compute the worst-case link budget for the HAPS-to-Gateway feeder at 38 GHz. The breakdown is: FSPL (Eq. 1) + rain (Eq. 3) + fog (Eq. 4) + gas (Eq. 5) + scintillation (Eq. 6), per ITU-R P.618 design guidance~\cite{itu_p618}.

\begin{table}[t]
\centering
\caption{Atmospheric Effects and ITU-R Recommendations for HAPS feeder links (38 GHz)}
\label{tab:atmospheric_effects}
\vspace{2mm}
{\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{|>{\raggedright\arraybackslash}p{0.27\columnwidth}|>{\raggedright\arraybackslash}p{0.16\columnwidth}|>{\raggedright\arraybackslash}X|}
\hline
\textbf{Atmospheric Effect} & \textbf{Affected Frequency} & \textbf{ITU Recommendation}  \\
\hline
Rain attenuation & $>$1 GHz & ITU-R P.618 (Earth–space path prediction), P.837 (rain climate statistics / rain rate), P.838 (specific attenuation)\\
\hline
Clouds and fog & 1--200 GHz & ITU-R P.840\\
\hline
Atmospheric gases (oxygen and water vapour) & 1--1000 GHz & ITU-R P.676\\
\hline
Atmospheric profiles (pressure, temperature, humidity) & all bands & ITU-R P.835\\
\hline
Surface water vapour statistics & all bands & ITU-R P.836\\
\hline
Tropospheric scintillation & 1--100 GHz & ITU-R P.618\\
\hline
\end{tabularx}}
\end{table}

% =====================================================================
\subsection{III.C: 2 GHz Service/Relay Link Model (UAV-to-User Air-to-Ground)}
% =====================================================================

Service and relay links operate at 2--2.2 GHz (S-band per 3GPP TS 38.821), connecting ground users either directly to HAPS (service link, high elevation angle) or via UAV relays at 300 m altitude (relay link). Unlike the 38 GHz feeder link, the 2 GHz S-band is resilient to atmospheric effects (fog, rain, gas absorption all negligible per ITU-R P.618) but highly sensitive to building blockage. This section models LoS probability, path loss, and multi-node interference.

\subsubsection{UAV air-to-ground link: LoS probability}
For a UAV $u$ at horizontal distance $r_{u,k}$ from user $k$, the LoS
probability follows the exact building-blockage product series
of~\cite{arani2023}, originally attributed to~\cite{holispechac2008} (IEEE Trans. Antennas Propag., 2008, vol. 56, no. 4, pp. 1078--1084):
\begin{equation}
P^\mathrm{LoS}_{u,k} = \prod_{n=0}^{J}\left[1-\exp\!\left(-\frac{1}{2\xi^2}
\Big(h_u-\tfrac{n+\frac12}{J+1}(h_u-h_k)\Big)^{2}\right)\right],
\label{eq:los}
\end{equation}

\noindent\textbf{Equation (8) --- Building-Blockage LoS Probability~\cite{holispechac2008}}: Exact product-series model~\cite{holispechac2008} for line-of-sight probability accounting for cumulative building blockage along the path. The product iterates over building layers from ground to UAV height.

\begin{equation}
J = \left\lfloor \frac{r_{u,k}}{1000}\sqrt{\alpha\beta} - 1 \right\rfloor,
\label{eq:losJ}
\end{equation}

\noindent\textbf{Equation (9) --- Number of Building Layers~\cite{holispechac2008}}: Computed from horizontal distance $r_{u,k}$ (meters), building density $\beta$ [buildings/km$^2$], and built-up ratio $\alpha$ [dimensionless] per Holis \& Pechac Table I~\cite{holispechac2008}.

\noindent Parameters from Table I (Holis \& Pechac 2008, IEEE Trans. Antennas Propag.~\cite{holispechac2008}): $P_\mathrm{NLoS}=1-P_\mathrm{LoS}$; $\alpha$ is the built-up land ratio,
$\beta$ the mean building density [buildings/km$^2$], and $\xi$ a building-height parameter [m]. All are
environment-dependent: for urban areas~\cite{holispechac2008},
$\alpha=0.3$, $\beta=500$, $\xi=15$\,m; dense urban uses $\alpha=0.5$, $\beta=300$, $\xi=20$\,m;
suburban $\alpha=0.1$, $\beta=750$, $\xi=8$\,m; high-rise $\alpha=0.5$, $\beta=300$, $\xi=50$\,m (Table I, cited in 300+ subsequent works)~\cite{holispechac2008}.

\subsubsection{Average path loss and SINR}
Unlike terrestrial base stations, HAPS nodes operate at high elevation angles. This near-zenith positioning provides a superior Line-of-Sight (LoS) probability, significantly reducing multi-path fading and shadowing in urban and mountainous terrain.
Each state $z\in\{\mathrm{LoS},\mathrm{NLoS}\}$ adds a mean excess loss $\eta_z$;
the two are probability-averaged:
\begin{align}
\mathrm{PL}_z &= 20\log_{10}\!\Big(\tfrac{4\pi f d_\mathrm{3D}}{c}\Big) + \eta_z,\\
\overline{\mathrm{PL}} &= P_\mathrm{LoS}\,\mathrm{PL}_\mathrm{LoS}
                        + P_\mathrm{NLoS}\,\mathrm{PL}_\mathrm{NLoS},
\label{eq:pl}
\end{align}

\noindent\textbf{Equation (10) --- Probability-Averaged Path Loss}: The first line computes path loss (free-space FSPL plus environment-dependent excess $\eta_z$) for LoS and NLoS states separately. The second line probability-averages over the LoS state (Eq. 8). The 3D distance is $d_\mathrm{3D}=\sqrt{r^2+(h_u-h_k)^2}$ where $r$ is horizontal distance and $h_u, h_k$ are UAV and user altitudes respectively.

The received power follows the standard link-budget relation
\begin{equation}
P_r = P_t + G_t + G_r - PL(d,\theta,\mathrm{state}),
\label{eq:rx-power}
\end{equation}
where $P_t$ is transmit power, $G_t$ and $G_r$ are the transmit and receive antenna gains, and $PL(d,\theta,\mathrm{state})$ is the path loss for the current geometry and propagation state. A blocked or NLoS link therefore yields a smaller $P_r$ through a larger path-loss term (equivalently, a lower channel gain $g_{u,k}$); it is not added as a separate degradation term in the SINR expression itself. The reduced signal power is already embedded in the SINR numerator before interference and noise are considered.

The downlink SINR is
\begin{equation}
\gamma_{u,k} = \frac{p_u\,g_{u,k}}{\sum_{u'\neq u} p_{u'} g_{u',k} + \sigma_0^2},
\qquad g_{u,k}=10^{-\overline{\mathrm{PL}}/10},
\label{eq:sinr}
\end{equation}

\noindent\textbf{Equation (11) --- Signal-to-Interference-plus-Noise Ratio (SINR)}: Computes downlink SINR at user $k$ from UAV $u$ per standard information-theory formulation. $p_u$ is transmit power [Watts], $g_{u,k}=10^{-\overline{\mathrm{PL}}/10}$ is channel gain (reciprocal of path loss in linear scale), interference sums over competing UAVs $u'$, and $\sigma_0^2$ is thermal noise power [Watts]. For the noise-limited characterization below, interference is negligible; the characterisation below is
noise-limited.

\subsection{III.D: Multi-HAPS User Association -- State and Baseline Policies}
\label{sec:assoc-baseline-setup}

Building on the 3-HAPS constellation (Table~\ref{tab:config}) and the SINR model of
Sections~III.A--III.C, this section defines a per-user association experiment: each
of $N=50$ users, once per simulated hour, observes its SINR to each of the 3 HAPS
(dB, Eq.~\eqref{eq:sinr}), its horizontal distance to each (km), each HAPS's battery
state of charge (\%), and the current weather (rain rate, visibility, hour of day).
One episode is one simulated day (24 hourly steps). Per episode, user positions and
each HAPS's initial battery SoC ($\mathcal{U}(30,100)\,\%$) are drawn once and held
fixed; weather is drawn once per episode (\texttt{WeatherScenarioGenerator},
\texttt{random\_mix} mode). Each HAPS's battery is charged by a fixed diurnal solar
profile and discharged by a constant platform/feeder draw plus its currently-associated
users' share of load ($P_{k,\text{user}}=1$~W per user, ASSUMED); the solar/platform
terms dominate, so time-of-day remains the primary driver of SoC and the association
policy has a secondary, but non-trivial, effect on it. Rain de-rates only the
feeder-link throughput credit (Section~\ref{subsec:multinode-sinr}), not the 2~GHz
service SINR.

Two closed-form (non-learned) association policies are evaluated here:
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \textbf{Random}: uniform-random platform choice each hour (sanity floor).
  \item \textbf{Max-SINR}: each user always associates to $\arg\max_p \text{SINR}_{k,p}$
    -- pure closed-form physics, no memory of battery or fairness state, no learning.
\end{itemize}
Both are evaluated over 50 held-out episodes (seed 999) using Jain's fairness index,
outage fraction (SINR below $-2$~dB, the severe-outage threshold measured in
Section~IV.B/Table A.3), and aggregate throughput (Shannon capacity, summed over
users) as metrics. Section~\ref{sec:assoc-baseline-results} reports the resulting
comparison; Section~\ref{sec:phase2-motivation} (Phase 2) later adopts this same
per-user state and evaluates whether a learned policy can improve further on the
closed-form baseline established there.

% =====================================================================
\section{Results}
% =====================================================================

This section presents results organized by frequency band applicability, mirroring the system model structure of Section~III:
(A) common propagation baseline (all frequency bands), (B) 38 GHz feeder link atmospheric losses, and (C) 2 GHz service/relay link propagation physics.

% =====================================================================
\subsection{IV.A: Common Propagation Baseline}
% =====================================================================

Both service links (2 GHz, §IV.C) and feeder links (38 GHz, §IV.B) are governed by the same universal equations for free-space path loss (Eq.~1) and scintillation fade depth (Eq.~12). This section establishes these common baseline models and evaluates them at both frequencies before band-specific atmospheric (feeder) and environmental (service) effects are layered on top.

\subsubsection{Free-space path loss (Eq.~1)}

\begin{figure}[h!]\centering
\includegraphics[width=0.95\columnwidth]{./figures/1b_haps_fspl_2ghz_vs_38ghz.png}
\vspace{3mm}
\caption{HAPS free-space path loss: frequency comparison (2~GHz service link vs.\ 38~GHz feeder link), \eqref{eq:fspl}.
Plotted in dB scale over 0--100\,km service footprint.
Solid line (blue): 2\,GHz service link; Dashed line (purple): 38\,GHz feeder link.
The constant \textbf{25.6\,dB offset} (at all distances) arises from $20\log_{10}(38/2)$ frequency scaling.
At 100\,km ground distance: 2\,GHz FSPL = 138.6\,dB, 38\,GHz FSPL = 164.2\,dB.}
\label{fig:fspl_comparison}
\end{figure}

Figure~\ref{fig:fspl_comparison} establishes the frequency-dependent free-space path loss baseline per Equation~(1). It shows the remarkable invariance of HAPS path loss across the 100\,km service footprint due to the 20\,km altitude dominance over horizontal distance, while making explicit the frequency dependence: a constant 25.6\,dB offset between service (2 GHz) and feeder (38 GHz) frequencies at every distance, landscape-independent (\S III.C; Arani et al.\ Eq.\ 2). A comprehensive analysis across eight distances and both frequencies is provided in Appendix~\ref{sec:app-data-tables} (Table~\ref{table:app-pathloss-2ghz-38ghz}).

\textbf{Key insight}: The feeder link (38 GHz) carries a 25.6\,dB path-loss penalty vs.\ the service link (2 GHz). This offset is derived from Equation~\eqref{eq:fspl} (FSPL frequency dependence): subtracting the path loss at 2 GHz from 38 GHz eliminates the distance term, yielding $\Delta L = 20\log_{10}(f_{\text{38 GHz}} / f_{\text{2 GHz}}) = 20\log_{10}(38/2) \approx 25.58$~dB, a distance-invariant quantity. The 38 GHz band avoids catastrophic gas-absorption resonances at 22.2 GHz (water vapor) and 60 GHz (oxygen)~\cite{itu_p676}, minimizing atmospheric loss. This frequency penalty mandates higher feeder transmit power, larger antennas, or lower receiver noise figures.

\FloatBarrier

\subsubsection{Scintillation fade depth (Eq.~12)}

Tropospheric scintillation---rapid amplitude fluctuations caused by refractive-index turbulence in the lower atmosphere---follows a universal frequency-dependent formula per Equation~\eqref{eq:scint_fade} but exhibits vastly different fade depths at 2 GHz (service) and 38 GHz (feeder). The frequency exponent $(f/f_{\mathrm{ref}})^{0.7}$ is empirical (not theoretically derived); it represents the power-law frequency scaling of tropospheric turbulence validated through measurements in ITU-R P.618-13~\cite{itu_p618}.

Figure~\ref{fig:scint_fade_depth}\,(a) shows fade depth parametrically across elevation angles and time percentages. Figure~\ref{fig:scint_fade_depth}\,(b) directly compares the elevation-angle dependence, revealing consistent frequency scaling across all elevation angles: the fade depth ratio between 38~GHz and 2~GHz is exactly $2.882 = (38/2)^{0.7}$ at all elevations, confirming the frequency scaling exponent in Equation~\eqref{eq:scint_fade}. At reference conditions $(p=1\%, \theta=30\circ)$, this ratio yields 10.53~dB (38~GHz feeder) vs. 3.65~dB (2~GHz service), a 6.88~dB spread driven entirely by frequency.

\begin{figure}[ht]\centering
\begin{subfigure}[b]{0.45\columnwidth}\centering
\includegraphics[width=0.95\textwidth]{./figures/9a_scintillation_fade_vs_time.png}
\caption*{(a) vs.\ time percentage}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.45\columnwidth}\centering
\includegraphics[width=0.95\textwidth]{./figures/9b_scintillation_fade_vs_elev.png}
\caption*{(b) vs.\ elevation}
\end{subfigure}
\vspace{3mm}
\caption{Scintillation fade depth characteristics (ITU P.618): \textbf{(a)} Fade depth vs.\ time percentage at five elevation angles ($10\circ$--$60\circ$). \textbf{(b)} Fade depth vs.\ elevation angle at five time percentages ($0.1\%$--$10\%$). Parametric analysis validates frequency scaling $(\propto f^{0.7})$, elevation scaling $(\propto 1/\sin\theta)$, and time-percentage scaling $(\propto \sqrt{\log(1/p)})$.}
\label{fig:scint_fade_depth}
\end{figure}

\textbf{Key insight}: The feeder link (38~GHz) not only carries a 25.6\,dB path-loss penalty (from Eq.~\eqref{eq:fspl}: $20\log_{10}(38/2) \approx 25.58$~dB, see Figure~\ref{fig:fspl_comparison}), but also faces $2.882\times$ deeper scintillation fades. At the reference condition $(p=1\%, \theta=30\circ)$, the feeder margin requirement jumps from 3.65\,dB (service) to 10.53\,dB (feeder)---a 6.88\,dB increase solely from scintillation. This frequency-dependent margin scaling is a fundamental property of tropospheric turbulence and mandates more aggressive link budgets for millimeter-wave feeder paths, consistent with practical Ka-band system designs that use larger antennas or higher transmit power to overcome both FSPL and scintillation penalties.

% =====================================================================
\subsection{IV.B: 2 GHz Service/Relay Link (Building Blockage)}
% =====================================================================

\subsubsection{Path Loss, LoS Probability, and SINR}

Single-node 2~GHz service and relay links operate at 2--2.2~GHz per 3GPP, connecting ground users to HAPS directly (service link, high elevation angle) or via UAV relays at 300~m altitude (relay link). Unlike the 38~GHz feeder link, the 2~GHz S-band is resilient to atmospheric effects (fog, rain, gas absorption all negligible per ITU-R P.618) but highly sensitive to building blockage.

\begin{figure}[ht]\centering
% LoS probability subfigure commented out: redundant with Path Loss
% \begin{subfigure}[b]{0.49\columnwidth}
% \centering
% \includegraphics[width=\columnwidth]{figures/2_los_probability.png}
% \caption*{(a) LoS Probability}
% \end{subfigure}\hfill
\begin{subfigure}[b]{0.49\columnwidth}
\centering
\includegraphics[width=\columnwidth]{./figures/3_uav_pathloss.png}
\caption*{(a) Path Loss}
\end{subfigure}\\[1em]
\begin{subfigure}[b]{0.49\columnwidth}
\centering
\includegraphics[width=\columnwidth]{./figures/4_sinr.png}
\caption*{(b) SINR}
\end{subfigure}
\vspace{3mm}
\caption{2~GHz service/relay link propagation (UAV altitude 300\,m):
\textbf{(a)} Average air-to-ground path loss per Equation~\eqref{eq:pl}. Suburban lowest loss; high-rise sharp LoS/NLoS transitions and higher blockage loss.
\textbf{(b)} Noise-limited SINR per Equation~\eqref{eq:sinr}. SINR = $10^{-\mathrm{PL}/10}$ (inverse of path loss); graph is mirror image of (a). Suburban sustains usable SINR (>10\,dB) to 700\,m; high-rise degrades near-zero by 400\,m. Step structure from floor function \eqref{eq:losJ}.}
\label{fig:los_pl_sinr}
\end{figure}

\FloatBarrier

% =====================================================================
\subsection{IV.C: 38 GHz Feeder Link (Atmospheric and Multi-Node Effects)}
% =====================================================================

\subsubsection{Atmospheric Loss Analysis}

The feeder link (HAPS-to-Gateway, 38 GHz) experiences significant atmospheric attenuation
dominated by rain, with gas, fog, and scintillation as secondary effects. The atmospheric loss is
highly frequency-dependent per ITU-R P.838~\cite{itu_p838}, with catastrophic peaks at 22.235 GHz (water vapor)~\cite{itu_p676} and 59.8 GHz (oxygen)~\cite{itu_p676}.
Rain loss increases monotonically with frequency (P.838~\cite{itu_p838} coefficients $k$, $\alpha$ grow from 10 to 100 GHz),
so lower frequencies inherently have less rain attenuation. However, the feeder link cannot use the service link's 2 GHz band due to regulatory, link-budget, and capacity constraints:
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \textbf{Regulatory separation}: ITU-R allocates 2 GHz (S-band) for \emph{terrestrial mobile access} (3GPP) and 38--39.5 GHz (Ka-band) for \emph{fixed HAPS backhaul} (ITU-R M.2101). Spectrum efficiency and interference isolation require distinct bands.
  \item \textbf{Antenna directivity}: At 2 GHz, wavelength $\lambda = 15$\,cm; a 1 m antenna achieves only $\sim$3.2° beamwidth. At 38 GHz, $\lambda = 7.9$\,mm; same antenna achieves $\sim$0.2° beamwidth, enabling point-to-point backhaul with interference rejection.
  \item \textbf{Link budget over 102 km}: Feeder path loss at 2 GHz would be $\approx$ 138.5 dB (vs. 164 dB at 38 GHz). To meet same receiver power with 2 GHz would require either 25.5 dB higher transmit power (infeasible on aircraft, power-limited) or larger ground antenna array.
  \item \textbf{Backhaul capacity}: Higher frequencies enable wider bandwidth (Ka-band supports 1+ GHz channels); 2 GHz band is contended with terrestrial mobile networks.
\end{itemize}
Thus, 38 GHz is chosen as a \emph{frequency tradeoff}: higher rain loss than 2 GHz, but it avoids resonance peaks at 22.2 GHz and 60 GHz while enabling practical high-gain antennas and spectrum allocation per ITU-R M.2101~\cite{itu_m2101}.

Figure~\ref{fig:atm_frequency_nominal} shows atmospheric loss across 10--100 GHz for the nominal feeder
link as four individual subfigures: (a) Ka-band (10--40 GHz) with
water-vapor peak at 22.2 GHz, (b) Q-band (40--80 GHz) with oxygen peak at 60 GHz, (c) V-band (80--100 GHz)
in continuum region, and (d) full spectrum on log-scale showing relative severity of all peaks. This
frequency dependence is independent of elevation angle; the worst-case feeder link (11.3$^\circ$ elevation,
100 km service edge) exhibits the same frequency profile but with magnitudes roughly doubled due to longer
slant path (102 km vs.\ 54 km nominal), as quantified in Tables~\ref{tab:atm_losses_nominal} and
~\ref{tab:atm_losses_worstcase}.

\input{appendix_tables/atm_losses_nominal.tex}
\input{appendix_tables/atm_losses_worstcase.tex}

\begin{figure}[h!]\centering
\includegraphics[width=0.95\columnwidth]{./figures/fig_atm_loss_nominal_frequency.png}
\vspace{3mm}
\caption{Atmospheric loss vs.\ frequency, nominal case (50\,km horizontal, 10\,mm/h rain, 21.75$^\circ$ elevation, Delhi): \textbf{(a)} Ka-band (10--40\,GHz) showing the water-vapor resonance at 22.2\,GHz and the 38\,GHz HAPS feeder point; \textbf{(b)} Q-band (40--80\,GHz) showing the oxygen resonance at 60\,GHz; \textbf{(c)} V-band (80--100\,GHz), continuum region; \textbf{(d)} full 10--100\,GHz spectrum on a log scale, showing the 38\,GHz feeder band sits in a local minimum between both resonance peaks.}
\label{fig:atm_frequency_nominal}
\end{figure}


\subsubsection{Multi-Node Interference and SINR Effects (2~GHz Service Link Only)}

\label{subsec:multinode-sinr}

\textbf{Scope clarification:} This subsection addresses multi-HAPS interference effects that apply \textbf{exclusively to the 2~GHz service link}. The 38~GHz feeder link operates at short range (10--500 m) to a gateway in proximity to the serving HAPS nadir, with interfering HAPS $\sim$65~km away. Consequently, feeder-link interference is negligible (far below noise floor), and the feeder link is \textbf{noise-limited, not interference-limited} (see \S IV.C.1 paragraph ``Why multi-HAPS interference is not applicable to feeder links''). The following SINR formulation and calculations apply to service-link analysis only.

The single-node noise-limited analysis above assumes only one HAPS transmits at a
time. In a practical multi-HAPS network, multiple nodes may transmit simultaneously
on the same frequency, creating mutual interference. Although service links experience a
10--20\,dB SINR penalty from this interference, a 3-HAPS constellation provides critical advantages that
justify the interference trade-off for 2~GHz service-link users.

\paragraph{Multi-Node SINR Formula:}

We adopt the standard multi-node SINR formulation from Arani~\emph{et~al.}~(2023) and ITU-R P.618:

\begin{equation}
\gamma_{u,k} = \frac{P_u \cdot g_{u,k}}{N_0^2 + \sum_{u' \neq u} P_{u'} \cdot g_{u',k}}
\label{eq:sinr-multinode}
\end{equation}

\noindent In dB domain:

\begin{equation}
\text{SINR}_{\text{dB}} = 10\log_{10}\left(\frac{C}{N + I}\right)
\label{eq:sinr-db}
\end{equation}

\noindent Where:
\begin{itemize}
  \item $P_u$ = transmit power of serving HAPS $u$ [W]
  \item $g_{u,k} = 10^{-\overline{\mathrm{PL}}(u,k)/10}$ = channel gain from serving HAPS to user $k$ (linear scale)
  \item $N_0^2$ = thermal noise power [W]
  \item $\sum_{u' \neq u} P_{u'} \cdot g_{u',k}$ = aggregated interference power from all other HAPS
  \item $C$ = carrier power [W], $N$ = noise power [W], $I$ = total interference power [W]
\end{itemize}

\noindent \textbf{Critical implementation note:} Calculation must proceed in linear domain (Watts),
then convert result to dB. dB-domain arithmetic (e.g., subtracting SINR values) is not
equivalent to this linear-domain division and will produce incorrect results (Goldsmith 2005).

\paragraph{LoS blockage in path loss -- tier-dependent, not universal:}

As already introduced in Section~III, line-of-sight blockage is not modeled as an additional degradation term in the SINR expression itself. It is embedded in the path-loss model through the effective received power,
\begin{equation}
P_r = P_t + G_t + G_r - PL(d,\theta,\mathrm{state}),
\end{equation}
so a blocked or NLoS link simply corresponds to a larger $PL$ (or lower channel gain $g_{u,k}$), and the reduced signal power is already reflected in the SINR numerator before interference and noise are considered. This distinction is important: the propagation state affects the channel gain, not the algebraic form of the SINR ratio itself. There are two physically distinct tiers:

\begin{itemize}
  \item \textbf{HAPS tier (20\,km altitude) -- both the 2\,GHz direct service link and the 38\,GHz
    feeder link}: pure free-space path loss (Eq.~1), \textbf{with no LoS/NLoS blockage term at all},
    per Arani, Hu \& Zhu (2023) Eq.~2~\cite{arani2023}. We adopt Arani et al.'s free-space model for
    consistency with our primary source. This is exact only very close to nadir ($>85^\circ$
    elevation holds within $\sim$2\,km of nadir); at the 11--22$^\circ$ elevations that dominate
    beyond 50\,km, it is a simplification. Notably, Holis \& Pechac (2008) -- the source of the
    $(\alpha,\beta,\xi)$ parameters used below -- was itself written specifically for elevation-dependent
    shadowing \emph{via high-altitude platforms}, not low-altitude links, so it would be a natural
    candidate for modeling HAPS-tier shadowing more precisely; we defer that refinement to future
    work rather than mix it into the present Arani-consistent baseline. Consequently, SINR at this
    tier is \textbf{landscape-independent} in the current model -- suburban, urban, dense-urban, and
    high-rise all produce identical path loss and SINR (see Table~\ref{table:app-arani-table1} and
    Figures~\ref{fig:sinr-service-link},~\ref{fig:sinr-feeder-link}).
  \item \textbf{UAV/relay tier (300\,m altitude) only}: building blockage is applied here using the
    same product-series LoS-probability formula and $(\alpha,\beta,\xi)$ parameter table from
    Holis \& Pechac (2008) (Section~III.C, subsection ``UAV air-to-ground link: LoS probability'') --
    borrowed for this lower-altitude tier in the absence of a UAV-specific model in Arani et al.,
    not because the source model was originally derived for UAV altitudes. The average path loss
    is computed as a weighted blend:

\begin{equation}
\overline{\mathrm{PL}} = P_{\mathrm{LoS}} \cdot \mathrm{PL}_{\mathrm{LoS}} + P_{\mathrm{NLoS}} \cdot \mathrm{PL}_{\mathrm{NLoS}}
\label{eq:pathlos-blend}
\end{equation}

\noindent where each term is a function of $(r, h, \text{env})$.

\noindent    where $P_{\mathrm{LoS}}$ is computed from the building-blockage product series (Eq.~3,
    Section~III.C) using environment-specific parameters: $\alpha$ (built-up ratio), $\beta$ (building
    density), and $\xi$ (mean building height). $\mathrm{PL}_{\mathrm{NLoS}}$ is significantly higher
    than $\mathrm{PL}_{\mathrm{LoS}}$ (typically 15--30\,dB), so denser urban environments exhibit
    higher average path loss and lower received signal power \emph{at this tier only}. (Note: the
    $\eta_{\mathrm{los}}$/$\eta_{\mathrm{nlos}}$ constants currently used for this blend are flat dB
    offsets, not yet cross-checked against a dedicated source table -- see the Phase~2 note in
    \texttt{channel\_model.py}.)
\end{itemize}

This design ensures the SINR formula (Equation~\eqref{eq:sinr-multinode}) remains clean and, where
blockage applies, the physics are captured entirely through the antenna-to-antenna channel gain
$g_{u,k}$ -- never as a separate multiplicative penalty in the SINR equation itself.

Now turning to the constellation trade-off:

\begin{itemize}
  \item \textbf{Coverage redundancy}: If one HAPS fails or drifts, users remain
    connected to at least one other node. Single-node systems have no fallback.
  \item \textbf{100\,km footprint}: A 3-HAPS equilateral triangle ($\sim 60$--$70$\,km
    spacing) covers the full 100\,km service area. Single nodes cover only
    $\sim 50$\,km radius (half the footprint).
  \item \textbf{Coordinated transmission}: TDMA scheduling (each HAPS transmits in
    time slots) or FDMA (frequency division) eliminates simultaneous interference.
    The interference shown in Figure 5 represents the worst case (all 3 nodes
    on same channel); real systems use coordination.
  \item \textbf{Beamforming opportunity}: Directional antennas reduce interference
    by 10--15\,dB, recovering most of the lost SINR without additional spectrum.
  \item \textbf{Load balancing}: Multiple HAPS nodes distribute traffic, reducing
    congestion and per-user power requirements.
\end{itemize}

Figure~\ref{fig:app-constellation-sectors} shows this constellation geometry directly, together with
the resulting spatial SINR pattern it produces at r~=~50\,km: a good sector (away from both
interferers) and a bad sector (toward them), rather than a distance-only falloff.

\begin{figure}[h!]\centering
\includegraphics[width=0.85\columnwidth]{./figures/constellation_with_sectors.png}
\vspace{3mm}
\caption{HAPS constellation geometry (true 65\,km equilateral triangle: S at origin, I1 at $0^\circ$, I2 at $60^\circ$) with spatial SINR sectors at r~=~50\,km. Good sector (green, away from interferers, $\sim$120--290$^\circ$) vs.\ bad sector (red, toward interferers, $\sim$330--75$^\circ$). Best +2.54\,dB @ $210^\circ$; worst --7.40\,dB @ $0^\circ$.}
\label{fig:app-constellation-sectors}
\end{figure}

\FloatBarrier

The interference model below shows the penalty without coordination, motivating the
DRL agent's learning objective: find an optimal transmission schedule that maximizes
coverage and fairness while minimizing interference.

\paragraph{Measurement and validation}

Figure~\ref{fig:multinode-interference} quantifies the interference-induced SINR penalty across the 100 km service footprint. The penalty is defined relative to the no-interference benchmark and represents the loss in usable signal quality caused by the other HAPS nodes. In the dBm traces, the downward bend after roughly 60 km reflects the transition from an interference-limited regime to a path-loss-dominated regime: the desired received power falls faster than the interference term, so the absolute signal level continues to decay even though the interference contribution is no longer the dominant cause of degradation.

The line shows mean SINR, averaged across all 24 angular positions around the constellation; since all four landscapes produce numerically identical results at the HAPS tier (\S III.C, Arani et al.\ Eq.\ 2), a single line is shown rather than four overlapping ones. The shaded band shows the min--max spread from that angular variation, not measurement uncertainty. The right-hand axis shows the corresponding elevation angle at the user (arctan of HAPS altitude over horizontal distance). Mean SINR falls from $+6.3$\,dB at 10\,km to $-4.3$\,dB at 100\,km, but the shaded band widens with distance (1.95\,dB at 10\,km to 11.94\,dB at 75\,km) -- angular position relative to the 65\,km equilateral constellation dominates over pure distance decay beyond $\sim$30\,km. The network must employ TDMA scheduling, FDMA, beamforming, or power control to manage this interference and its spatial variability.

\begin{figure}[h!]\centering
\begin{subfigure}[b]{0.49\columnwidth}\centering
\includegraphics[width=\linewidth]{./figures/figure5_multi_haps_sinr_mean.png}
\caption{Mean SINR and angular min--max spread across 100\,km service footprint.}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.49\columnwidth}\centering
\includegraphics[width=\linewidth]{./figures/figure5_model_received_power.png}
\caption{Received-power comparison showing clean vs. effective power under three-HAPS interference.}
\end{subfigure}
\vspace{3mm}
\caption{Multi-HAPS interference characterization at the service link: (a) SINR variability and (b) received-power degradation.}
\label{fig:multinode-interference}
\end{figure}

\FloatBarrier

Figure~\ref{fig:sinr-service-link} shows SINR for the 2~GHz service link along one representative direction from nadir, in four identical subplots (suburban, urban, dense\_urban, high\_rise -- shown separately only to make the landscape-independence visually explicit). It has SINR\_Single (solid blue bars, ideal with no interference), SINR\_Actual (hatched blue bars, with 3-node interference), and SINR\_Interferers (red line, interference-to-noise ratio as a function of horizontal distance). All calculations use linear-domain SINR per Equation~\eqref{eq:sinr-multinode}, pure free-space path loss without any landscape term (\S III.C; Arani et al.\ Eq.\ 2). Key observations: (1) SINR\_Interferers stays positive (0.95--4.6 \, dB) throughout the entire 10--100 km range shown -- the link remains interference-limited throughout; it does not cross into a noise-limited regime within this footprint. (2) SINR\_Actual crosses from positive to negative between 30\,km ($+2.0$\,dB) and 50\,km ($-1.6$\,dB), reaching $-4.3$\,dB at 100\,km, reflecting that path loss to the serving HAPS eventually dominates the link budget and interference itself is not the limiting factor. (3) All four subplots are numerically identical -- there is no landscape effect at this tier.

\begin{figure}[h!]\centering
\includegraphics[width=0.95\columnwidth]{./figures/haps_sinr_trellis.png}
\vspace{3mm}
\caption{2~GHz service link SINR, four identical landscape subplots (landscape-independent, \S III.C), 10--100\,km distances.}
\label{fig:sinr-service-link}
\end{figure}


Figure~\ref{fig:sinr-feeder-link} shows the SINR performance of the 38 GHz HAPS-to-gateway feeder link for gateway horizontal distances of 10--500 m. The gateway is assumed to be at an altitude of 50 m, while the HAPS operates at its nominal stratospheric altitude. The resulting SINR is primarily noise-limited and remains approximately constant at $-7.2$\,dB across all considered distances. This behavior is expected because the change in horizontal distance from 10 m to 500 m produces a negligible change in the HAPS-to-gateway slant range relative to the HAPS altitude.


\begin{figure}[h!]\centering
\includegraphics[width=0.95\columnwidth]{./figures/haps_feeder_link_38ghz.png}
\vspace{3mm}
\caption{38~GHz feeder link (HAPS-to-Gateway) SINR, four identical landscape subplots (landscape-independent, \S III.C), 10--500\,m distances.}
\label{fig:sinr-feeder-link}
\end{figure}

\FloatBarrier

\paragraph{Why multi-HAPS interference is not applicable to feeder links:}
The key distinction from service-link analysis is that the feeder link operates at extremely short range (0--0.5 km, typically 10--500 m), placing the gateway in \emph{proximity to the nadir} of the serving HAPS. In contrast, the interfering HAPS in the constellation are positioned approximately 65~km away from the serving HAPS (equilateral-triangle geometry, \S III.A). Consequently, the path-loss attenuation to the interfering HAPS is so severe ($\sim$120~dB at 38~GHz over 65~km) that their interference contribution remains \emph{negligibly small} compared to the receiver's thermal noise level ($-100$\,dBm assumed floor). Therefore, the feeder link is \textbf{noise-limited, not interference-limited}, unlike the service link. The resulting SINR degradation is driven primarily by the background receiver noise, not by multi-HAPS interference from the constellation.

A Johnson--Nyquist thermal-noise model, $N=kTB$, or equivalently $N_{\text{dBm}}=-174+10\log_{10}(B)+NF$, would provide a more physically grounded noise estimate than the assumed flat $-100$\,dBm noise floor. However, for this short-range analysis, replacing the assumed noise floor with a bandwidth- and receiver-dependent thermal-noise value would primarily change the absolute SINR level rather than its nearly flat variation with distance. A more detailed link-budget analysis incorporating the actual channel bandwidth, receiver noise figure, directional gateway antenna gain, and HAPS transmit power is therefore deferred to the next phase. 


\paragraph{2~GHz Service Link: Distance dependence}
When averaged over all angular positions relative to the constellation, the mean SINR decreases from $+6.3$\,dB at $r=10$\,km to $+2.0$\,dB at $r=30$\,km, falls below 0\,dB between 30 and 50\,km, and reaches $-4.3$\,dB at $r=100$\,km (Figure~\ref{fig:multinode-interference}). The interference-to-noise ratio (SINR\_Interferers) remains positive, ranging from 0.95 to 4.6\,dB, throughout the 10--100\,km range.

\paragraph{2~GHz Service Link: Angular (constellation-geometry) dependence}
At a given horizontal distance, the SINR exhibits substantially greater variation with angular position than with distance alone. For example, at $r=50$\,km, the SINR ranges from $-7.40$\,dB when the receiver is oriented toward interferer I1 ($\theta=0^\circ$) to $+2.54$\,dB when positioned away from the constellation ($\theta=210^\circ$), corresponding to a 9.94\,dB spatial spread (Tables A.3a--d, Appendix). This pronounced spatial variation is driven by the constellation geometry rather than the landscape environment.

\subsubsection{Service Link (2~GHz): SINR Effects from Distance and Constellation Geometry}

The SINR degradation due to multi-HAPS interference at \textbf{2~GHz service links} is governed primarily by two factors: \textbf{(i)} the horizontal distance $r$ from the HAPS nadir, and \textbf{(ii)} the angular position of the receiver relative to the two interfering HAPS in the 65~km equilateral-triangle constellation (\S\ref{subsec:multinode-sinr}, Figure~\ref{fig:app-constellation-sectors}). The landscape environment does \emph{not} influence the SINR at this frequency, since both the desired and interfering links are modeled using free-space path loss (\S III.C; Arani et al.\ Eq.\ 2). Consequently, the suburban, urban, dense-urban, and high-rise environments produce identical SINR values, as confirmed numerically in Table~\ref{table:app-sinr-multinode-distances} and Figure~\ref{fig:multinode-interference}.

This simulation of 2~GHz service-link interference therefore demonstrates that distance alone is insufficient to characterize interference conditions and motivates the use of a DRL-based approach for adaptive scheduling.

\subsection{Scintillation Fading: Implementation and Results}
\label{subsec:scintillation}

Tropospheric scintillation (introduced in Section~III.B) is characterized in figures (Plots 9, 10, 11). This subsection is presented the the ITU-R P.618
fade-depth model that depends on empirical time-percentage statistics most meaningfully explained through parametric plots showing the dependencies on frequency, elevation angle,
and availability target. The deterministic fade-depth formula (Equation~\eqref{eq:scint_fade}) and stochastic fading envelope realizations are therefore grouped together with their
visualization and interpretation in the Results section for pedagogical clarity.

\paragraph{Fade depth model (ITU P.618).}
Following the empirical approach in ITU-R P.618-13 Annex 2, the scintillation
fade depth is computed as a function of time percentage~$p$, elevation angle~$\theta$,
and frequency~$f$:
\begin{equation}
D(p, \theta, f) = D_{\mathrm{ref}} \cdot \left(\frac{f}{f_{\mathrm{ref}}}\right)^{0.7}
                 \cdot \frac{1}{\sin(\theta)} \cdot \sqrt{\log(1/p)} \quad [\mathrm{dB}],
\label{eq:scint_fade}
\end{equation}
where:
\begin{itemize}\setlength{\itemsep}{0pt}
  \item $p$ = time percentage (availability criterion, e.g., $p=1\%$ means fade depth exceeded 1\% of the time, or 99\% availability)
  \item $\theta$ = elevation angle from receiver to transmitter [degrees] (lower angles $\rightarrow$ longer atmospheric path, higher fading)
  \item $D_{\mathrm{ref}} = 2.5\,\mathrm{dB}$ = reference fade depth at K-band ($f_{\mathrm{ref}}=20\,\mathrm{GHz}$, $p=1\%$, $\theta=30^\circ$) per ITU-R P.618-13 Annex 2
  \item Exponent $0.7$ is \textbf{empirical} (not theoretically derived), validated through radiowave propagation measurements
\end{itemize}
The elevation scaling $1/\sin(\theta)$ reflects the longer atmospheric path at lower angles; near-zenith links ($\theta \approx 90^\circ$) have minimal fading, while grazing links ($\theta$ small) face severe turbulence.

\textbf{Reference fade depths at K-band (20 GHz, $\theta=30^\circ$, per ITU-R P.618)}:
\begin{itemize}
  \item $p=0.1\%$: $D_{\mathrm{ref}} \approx 13.14\,\mathrm{dB}$ (99.9\% availability target)
  \item $p=1.0\%$: $D_{\mathrm{ref}} \approx 10.73\,\mathrm{dB}$ (standard planning margin)
  \item $p=10.0\%$: $D_{\mathrm{ref}} \approx 7.59\,\mathrm{dB}$ (conservative)
\end{itemize}

These are \textbf{reference values at 20 GHz}. Actual fade depths for service (2 GHz) and feeder (38 GHz) links must be scaled using Equation~\eqref{eq:scint_fade} with the frequency factor $(f/f_{\mathrm{ref}})^{0.7}$:
\begin{itemize}
  \item \textbf{Service link (2 GHz)}: $(2/20)^{0.7} \approx 0.156$, so $D_{\mathrm{service}} \approx 0.156 \times D_{\mathrm{ref}}$ (lower fade depths, less severe fading)
  \item \textbf{Feeder link (38 GHz)}: $(38/20)^{0.7} \approx 1.50$, so $D_{\mathrm{feeder}} \approx 1.50 \times D_{\mathrm{ref}}$ (higher fade depths, more severe fading)
\end{itemize}

\paragraph{Fading envelope generation.}
For time-series simulations, random fading samples are drawn fromr Gamma distributions, scaled to match the ITU fade-depth statistics at each time percentage. Log-normal fading (small-scale, smooth envelope) is typical for terrestrial
channels; Gamma fading (steep tail, severe events) models more extreme tropospheric
turbulence. Figure~\ref{fig:scint_timeseries} shows 1000 sample realizations at four
time percentages (0.1\%, 1\%, 5\%, 10\%), comparing both distributions against the
ITU P.618 fade-depth reference line. Both are used to explore sensitivity to fading
model assumptions.

\begin{figure}[ht]\centering
\begin{subfigure}[b]{0.45\columnwidth}
\centering
\includegraphics[width=0.95\textwidth]{./figures/11a_fading_envelope_timeseries.png}
\caption*{(a) $p = 0.1\%$}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.45\columnwidth}
\centering
\includegraphics[width=0.95\textwidth]{./figures/11b_fading_envelope_timeseries.png}
\caption*{(b) $p = 1\%$}
\end{subfigure}\\[1em]
\begin{subfigure}[b]{0.45\columnwidth}
\centering
\includegraphics[width=0.95\textwidth]{./figures/11c_fading_envelope_timeseries.png}
\caption*{(c) $p = 5\%$}
\end{subfigure}\hfill
\begin{subfigure}[b]{0.45\columnwidth}
\centering
\includegraphics[width=0.95\textwidth]{./figures/11d_fading_envelope_timeseries.png}
\caption*{(d) $p = 10\%$}
\end{subfigure}
\vspace{3mm}
\caption{Scintillation fading envelope (1000 samples, four time percentages): Blue (log-normal), Red (Gamma), Green dashed (ITU P.618 reference).}
\label{fig:scint_timeseries}
\end{figure}


\paragraph{Integration into feeder link loss (Eq.~\ref{eq:feeder_loss}).}
The total feeder link loss now becomes:
\begin{equation}
L_{\mathrm{total}} = L_{\mathrm{FSPL}} + A_{\mathrm{rain}} + A_{\mathrm{cloud/fog}}
                    + A_{\mathrm{gas}} + D(p, \theta, f) \quad [\mathrm{dB}],
\label{eq:feeder_loss_scint}
\end{equation}
where $D(p, \theta, f)$ from \eqref{eq:scint_fade} replaces the generic placeholder
in \eqref{eq:feeder_loss}. The scintillation margin is frequency-dependent (higher at
Ka-band than K-band) and elevation-dependent (sharper at low angles), making it critical
for link budgets in both service and feeder paths.

Figure~\ref{fig:scint_link_loss} compares deterministic vs.\ faded feeder link loss: (a) loss envelope
over distance (20--200 km), showing the 7--13 dB scintillation margin as a shaded
region, and (b) attenuation margin as a function of time percentage, annotated with
key values at 0.1\%, 1\%, and 10\%. The visualizations reveal that scintillation
introduces a power budget gap that scales nonlinearly with availability target.


\begin{figure}[ht]\centering
\begin{subfigure}[b]{0.45\columnwidth}\centering
\includegraphics[width=0.95\textwidth]{./figures/10a_feeder_loss_vs_distance.png}
\caption*{(a) loss vs.\ distance}
\end{subfigure}
\hfill
\begin{subfigure}[b]{0.45\columnwidth}\centering
\includegraphics[width=0.95\textwidth]{./figures/10b_scintillation_margin.png}
\caption*{(b) margin vs.\ time percentage}
\end{subfigure}
\vspace{3mm}
\caption{Feeder link loss: deterministic vs.\ scintillation-faded (ITU P.618): \textbf{(a)} Total loss over distance (20--200 km) showing scintillation envelope. \textbf{(b)} Attenuation margin vs.\ time percentage ($0.1\%$--$10\%$), annotated with key values. Scintillation introduces 7.6--13.1 dB power budget gap across availability range.}
\label{fig:scint_link_loss}
\end{figure}

\FloatBarrier

\subsection{IV.D: Multi-HAPS Association Baseline Results (Random vs.\ Max-SINR)}
\label{sec:assoc-baseline-results}

Using the setup of Section~\ref{sec:assoc-baseline-setup}, this measures the two
closed-form association policies against each other. All numbers are measured over
50 held-out 24-hour episodes (seed 999), 50 users.

\begin{table}[t]
\centering
\caption{Association baseline results: 50 held-out 24-hour episodes (seed 999), 50 users, mean $\pm$ std}
\label{tab:assoc-baseline-results}
{\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{|>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|}
\hline
\textbf{Policy} & \textbf{Jain} & \textbf{Outage /50} & \textbf{Throughput [bits/s/Hz]}\\
\hline
Random & $0.331\pm0.046$ & $33.7\pm3.3$ & $33.95\pm4.75$ \\
\hline
Max-SINR & $\mathbf{0.700\pm0.027}$ & $\mathbf{1.0\pm0.9}$ & $\mathbf{76.18\pm3.57}$ \\
\hline
\end{tabularx}}
\end{table}

Even this simple, closed-form per-user rule -- always associate to whichever HAPS
gives the best instantaneous SINR -- more than doubles Jain fairness, cuts outage by
$34\times$ (33.7 to 1.0 of 50 users), and more than doubles aggregate throughput
relative to random association. This confirms, at the level of a full multi-user
network simulation, what the spatial SINR heterogeneity in Table A.3 already implied:
association choice matters far more than any fixed or naive assignment. Section
\ref{sec:phase2-motivation} (Phase 2) evaluates whether a learned policy can improve
further on this closed-form baseline.

% =====================================================================
\section{Conclusion}
% =====================================================================

\paragraph{Propagation metrics and path-loss characterisation.}
The four core propagation metrics---HAPS free-space path loss, UAV LoS probability,
average air-to-ground path loss, and single-node SINR---are implemented and
characterised through Figures~\ref{fig:fspl_comparison}--\ref{fig:los_pl_sinr}.

\paragraph{Multi-HAPS interference and landscape effects.}
The addition of multi-HAPS interference analysis (Figure~\ref{fig:multinode-interference}) characterizes realistic simultaneous-transmission
scenarios across all four landscape environments (suburban, urban, dense-urban, high-rise).
The analysis reveals that SINR behavior is landscape-dependent for \textbf{2 GHz service links}:
suburban areas maintain high LoS and sustain usable SINR to greater distances, while high-rise dense cities experience
sharp LoS collapse and interference-dominated regimes at short distances. Across all service-link landscapes,
simultaneous transmission from three HAPS nodes incurs a 10--20\,dB SINR penalty (verified in Figure~\ref{fig:multinode-interference}).

\noindent \textbf{Feeder link (38 GHz) does NOT experience this penalty}: The three interfering HAPS are $\approx 65$ km from the ground Gateway, placing them far outside the 10--500 m feeder coverage. Consequently, interference from constellation members remains well below thermal noise floor; feeder SINR is primarily noise-limited and approximately constant at $-7.2$~dB (Section~\ref{subsec:feeder-sinr}, Figure~\ref{fig:sinr-feeder-link}).

\paragraph{Scintillation fading (ITU P.618 implementation).}
Sections~\ref{subsec:scintillation} and Figures~\ref{fig:scint_fade_depth}--\ref{fig:scint_timeseries} introduce a physics-based model of tropospheric scintillation affecting both service links (UE $\leftrightarrow$ HAPS, 5$^\circ$--85$^\circ$ elevation-dependent) and feeder links (HAPS $\leftrightarrow$ Gateway, 25$^\circ$--45$^\circ$ fixed). The fade-depth model
\eqref{eq:scint_fade} accounts for frequency scaling ($\propto f^{0.7}$), elevation scaling
($\propto 1/\sin(\theta)$), and time-percentage statistics ($\propto \sqrt{\log(1/p)}$), directly from ITU-R P.618-13. Two distribution models (log-normal and Gamma) enable exploration of fading assumptions. Typical S-band (2~GHz) margins range 2.6--4.7\,dB across the 0.1\%--10\% time-percentage range, while Ka-band (38~GHz) margins range 7.6--13.1\,dB, reflecting the frequency dependence of tropospheric turbulence.


\vspace{3mm}

\noindent\textbf{Key Findings:}
\begin{itemize}
\item \textbf{9.94\,dB variation @ r~=~50\,km:} across the full 24-point angular sweep, SINR ranges --7.40\,dB (worst, $\theta=0^\circ$, toward I1) to +2.54\,dB (best, $\theta=210^\circ$)—vastly exceeds distance-based degradation. Note the asymmetric equilateral geometry (I1 @ $0^\circ$, I2 @ $60^\circ$) means the true best/worst angles are not the cardinal directions shown in Tables A.3a--d; see Figure~\ref{fig:app-constellation-sectors} for the good/bad sector layout.
\item \textbf{Spatial heterogeneity dominates r~$>$~30\,km:} At r~=~50\,km, 9.94\,dB spatial variation exceeds ~2--3\,dB per distance doubling.
\item \textbf{DRL scheduling necessity:} Distance-only assignment fails. Must learn angle-dependent interference patterns.
\item \textbf{Sector structure:} Good sector ($\sim$120--290$^\circ$, SINR~$>$~0\,dB); bad sector ($\sim$330--75$^\circ$, SINR~$<$~--5\,dB within roughly $345$--$60^\circ$).
\end{itemize}

% =====================================================================
\newpage

\section{Phase 2: DRL for User Association}
% =====================================================================

Phase~2 introduces a Deep Q-Network (DQN) agent for dynamic user-to-platform association in the HAPS--UAV--Gateway network. Whereas Phase~1 characterized static link quality, Phase~2 learns real-time association decisions maximizing throughput, fairness, and energy efficiency.

\subsection{Motivation: Why DRL?}
\label{sec:phase2-motivation}

The spatial SINR heterogeneity discovered in Phase~1 (9.94\,dB variation at 50\,km driven by constellation geometry alone, Figure~\ref{fig:multinode-interference}) demonstrates that distance-only scheduling heuristics fail. A simple ``always pick max SINR'' rule cannot anticipate or adapt to the angular SINR patterns created by the 3-HAPS triangle. Moreover, when weather degrades feeder links (Section~\ref{subsec:multinode-sinr}), the network must dynamically fail-over users from one platform to another while respecting battery constraints on each HAPS node.

DRL offers a principled approach: the agent observes the current network state (SINR to each platform, battery levels, weather forecast) and learns a policy $\pi(a | s)$ that selects the best platform for each user, maximizing cumulative reward. By training on diverse scenarios (rain events, user mobility, multi-node interference), the learned policy discovers non-obvious patterns (e.g., preemptively rerouting from a well-positioned but rain-threatened link to a less ideal but stable backup) that no hand-crafted rule would capture.

\subsection{Per-User Markov Decision Process (MDP) Formulation}

The model has 4 platform choices (HAPS\_1, HAPS\_2, HAPS\_3, UAV\_relay). With N users, a centralized MDP would face $4^N$ possible joint assignments—intractable for large networks. We instead decompose into per-user MDPs: each user independently chooses a platform, and the network computes cumulative reward after all assignments.

\subsubsection{State Space}

The state vector for user~$k$ is 12-dimensional:
\begin{align}
\mathbf{s}_k &= [\text{SINR}_{k,1,2,3}, r_{k,1,2,3}, \text{SoC}_{1,2,3},\nonumber\\
&\quad R_{\text{rain}}, V_{\text{vis}}, t_{\text{hour}} ]
\label{eq:state-vector}
\end{align}

\noindent \textbf{Feature Definitions}
(cross-referenced to Phase~1 results):

\begin{itemize}\setlength{\itemsep}{0pt}
  \item $\text{SINR}_{k,p}$: Downlink SINR at user $k$ from platform $p$ (dB). Phase~1 measured $[-7.4, +7.3]$~dB across 100~km footprint (Fig.~\ref{fig:multinode-interference}); bounds $[-20, +30]$~dB provide headroom for edge cases.
  \item $r_{k,p}$: Horizontal distance user~$k$ to platform~$p$ (km). Range $[0, 100]$~km covers service footprint (§~II).
  \item $\text{SoC}_p$: Battery state-of-charge platform~$p$ (\%). Observed by all users; range $[0, 100]\,\%$ (empty to full).
  \item $R_{\text{rain}}$: Rainfall rate (mm/h). Range $[0, 50]$~mm/h (rare events).
  \item $V_{\text{vis}}$: Visibility (m). Range $[0, 10]$~km (fog/clear).
  \item $t_{\text{hour}}$: Time-of-day (normalized). Range $[0, 1]$ maps 0~$\to$~midnight, 0.5~$\to$~noon.
\end{itemize}

\textbf{Observation Space Bounds} (negative SINR preserved for outage signals):
\begin{equation}
\mathcal{S} = \underbrace{[-20,+30]^3}_{\text{SINR}_{k,p}} \times \underbrace{[0,100]^3}_{r_{k,p}\text{ (km)}} \times \underbrace{[0,100]^3}_{\text{SoC}_p\text{ (\%)}} \times \underbrace{[0,50]}_{\text{rain}} \times \underbrace{[0,10k]}_{\text{vis (m)}} \times \underbrace{[0,1]}_{t}
\label{eq:obs-space}
\end{equation}

\noindent \textbf{Phase 2 DQN Scope} (scintillation deferred to Phase 3):
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \textbf{Included}: Deterministic SINR from constellation geometry (Phase~1); rainfall \& visibility for feeder margin planning; battery state for load balancing
  \item \textbf{NOT included}: Tropospheric scintillation fading (Section~\ref{subsec:scintillation}) --- time-varying random process requiring LSTM forecasting (Phase~3)
  \item \textbf{Approximation}: Phase~2 assumes worst-case static fading margin ($3.65$~dB service, $10.53$~dB feeder at $p=1\%$) or uses Phase~1 fade-depth statistics as penalty modulation
  \item \textbf{Phase 3}: LSTM weather forecaster will predict scintillation fading from rainfall/visibility timeseries, enabling adaptive margin reallocation
\end{itemize}

\textbf{Validation:} Phase~1 measured SINR $\in [-7.40, +7.30]$~dB over 100~km (Table A.3, worst/best-case constellation angles). Bounds $[-20, +30]$ dB provide $\pm 12.6$~dB margin for edge cases (atmospheric fading, relay paths in v2).

\subsubsection{Action Space}

Discrete platform choice: $\mathbf{a}_k \in \{0, 1, 2\}$ (HAPS1, HAPS2, HAPS3). UAV relay ($a=3$) deferred to Phase~2 v2.

\subsubsection{Reward Function}

All computations use \textbf{linear SINR} ($\gamma_k = 10^{\text{SINR}_{\text{dB}}/10}$), never dB arithmetic:
\begin{align}
R(\mathbf{a}) &= 0.5 L_{\text{agg}} + 0.3 J_{\text{fair}} - 0.2 P_{\text{eng}} + \text{Penalties}\\
L_{\text{agg}} &= \sum_{k=1}^N \log_2(1 + \gamma_k) \quad [\text{bits/s/Hz}]\\
J_{\text{fair}} &= \frac{(\sum_k \gamma_k)^2}{N \sum_k \gamma_k^2} \in [0,1]\\
P_{\text{eng}} &= \frac{1}{3}\sum_{p=1}^{3} \left(1 - \frac{\text{SoC}_p}{100}\right)
\end{align}

\noindent \textbf{Penalties} (thresholds calibrated to Phase~1 measured ranges; aggregated as network average, not per-user):
\begin{itemize}\setlength{\itemsep}{0pt}
  \item Severe outage: $-100 \times (\text{fraction of users in outage})$ if $\text{SINR}_{k,\text{dB}} < -2$~dB (well below average +2~dB)
  \item Degraded service: $-20 \times (\text{fraction of users degraded})$ if $-2 \le \text{SINR}_{k,\text{dB}} < 0$~dB (below average)
  \item Low-battery: $-50$ if $\text{SoC}_p < 10\,\%$ with $> 0$ assigned users
\end{itemize}

\noindent \textbf{Threshold Rationale:} Phase~1 measured: best $+7.3$~dB, average $+2.0$~dB, worst $-4.3$~dB. Penalties trigger below average ($< 0$~dB), not within normal operating range ($+2$ to $+7$~dB), allowing agent to reward good performance ($> +2$~dB) without false penalties.

\subsection{Critical Implementation Fixes}

Phase~2 training must avoid six silent errors:

\textbf{Error 1:} Jain's index on dB (wrong---log scale)
\begin{equation}
J_{\text{wrong}} = \frac{(\sum_k \text{SINR}_{k,\text{dB}})^2}{N \sum_k \text{SINR}_{k,\text{dB}}^2}
\end{equation}

\textbf{Fix:} Use linear scale
\begin{equation}
J_{\text{correct}} = \frac{(\sum_k 10^{\text{SINR}_k/10})^2}{N \sum_k (10^{\text{SINR}_k/10})^2}
\end{equation}

\textbf{Error 2:} SINR bounds $[0,100]$ hide outages.
\textbf{Fix:} Use $\text{SINR} \in [-20,+30]$~dB.

\vspace{3pt}

\textbf{Error 3:} HAPS solar from ground rain (HAPS at 20~km is above clouds).
\textbf{Fix:} Use diurnal cycle only, not weather.

\vspace{3pt}

\textbf{Error 4:} LSTM $(24,2) \to (60,)$ resolution mismatch.
\textbf{Fix:} Match resolution: $(24,2) \to (1,2)$ (hourly to hourly).

\vspace{3pt}

\textbf{Error 5:} Reward broadcast to all $N$ users identically (the fully-aggregated
network reward $R(\mathbf{a})$ given to every user's transition, regardless of that
user's own action). With $N=50$ simultaneously-acting users, any single user's choice
moves $R(\mathbf{a})$ by only a small fraction of its value, so the DQN cannot tell
which action caused the change -- training collapsed to a constant always-pick-HAPS-1
policy (identical evaluation reward/fairness/outage across 200+ consecutive training
episodes; verified by inspecting the learned policy's action distribution directly).
\textbf{Fix:} Decompose the additive components of $R(\mathbf{a})$ -- $L_{\text{agg}}$
and the outage penalties are literally sums (resp. fractions) over users -- into a
per-user share, so each user is credited with its own throughput/outage contribution.
$J_{\text{fair}}$ and $P_{\text{eng}}$ are genuinely network-level and remain a shared
term. This does not change the total reward mass, only its attribution for learning.

\vspace{3pt}

\textbf{Error 6:} HAPS battery SoC update treats a dimensionless energy fraction as if
it were already a percentage: $\text{SoC} \mathrel{+}= (\Delta E_{\text{Wh}} / C_{\text{battery}})$
instead of $\text{SoC} \mathrel{+}= 100 \times (\Delta E_{\text{Wh}} / C_{\text{battery}})$.
This under-scales every SoC update by $100\times$, so battery state barely moves
regardless of policy (observed: SoC pinned at $\approx$65\% across all three
Section~\ref{sec:phase2-results} policies before the fix) -- silently disabling the
entire energy/battery term of the reward. \textbf{Fix:} apply the missing $\times100$
conversion, and use $C_{\text{battery}}=12{,}000$~Wh matching Table~\ref{tab:config}'s
value (the module's own uncorrected default, 1000~Wh, is 12$\times$ too small and
produces implausible full-charge/full-drain swings within a single hour).

\subsection{HAPS Battery Model}

\subsubsection{Energy Balance}

State-of-charge updated hourly:
\begin{equation}
\Delta \text{SoC} = \frac{P_{\text{solar}}(t) - (P_{\text{feeder}} + P_{\text{payload}} + \sum_k P_{k,\text{user}})}{C_{\text{battery}}}
\label{eq:battery-balance}
\end{equation}

where: $P_{\text{feeder}} = 500$~W (transmitter), $P_{\text{payload}} = 200$~W (control), $P_{k,\text{user}}$ (assigned load), $C_{\text{battery}} = 12000$~Wh.

\vspace{3pt}

\subsubsection{Solar Power (Diurnal Cycle)}

\textbf{Critical principle:} HAPS at 20~km (stratosphere, above $>99\%$ clouds). Solar independent of ground weather.

\noindent Solar irradiance (cosine profile):
\begin{equation}
I_0(t_{\text{hr}}) = \begin{cases}
600 \cos(\pi(t_{\text{hr}} - 12)/12) & 6 \le t_{\text{hr}} \le 18 \\
0 & \text{night}
\end{cases} \quad [\text{W/m}^2]
\end{equation}

\noindent Generated power:
\begin{equation}
P_{\text{solar}}(t) = 20\,\text{m}^2 \times 0.20 \times I_0(t) = 2400\,\text{W (noon)}, \, 0\,\text{W (night)}
\end{equation}

\vspace{3pt}

\subsubsection{Operating Thresholds}

SoC clipped to $[0, 100]\,\%$:
\begin{itemize}\setlength{\itemsep}{2pt}
  \item \textbf{Charging:} Day (6 AM--6 PM) when $P_{\text{solar}} > P_{\text{load}}$
  \item \textbf{Draining:} Night when $P_{\text{solar}} = 0$
  \item \textbf{Critical:} SoC $< 5\,\%$ $\Rightarrow$ shutdown (no service)
  \item \textbf{Penalty:} SoC $< 10\,\%$ with users $\Rightarrow$ reward $-50$ (load balancing)
\end{itemize}

\textbf{Thresholds:} SoC $\in [0,100]\,\%$. Critical: $< 5\%$ (shutdown); penalty: $< 10\%$ (overload). Agent learns to balance users across platforms to avoid over-draining during night.

\subsection{DQN Results and Validation Against the Closed-Form Baseline}
\label{sec:phase2-results}

This section trains a DQN on the state and action space defined above (adopted
directly from Section~\ref{sec:assoc-baseline-setup}) and evaluates it against the
closed-form Random/Max-SINR baseline already established in Section~IV.D
(Table~\ref{tab:assoc-baseline-results}) -- the question this paper's own Motivation
(Section~\ref{sec:phase2-motivation}) raises but does not itself measure. All numbers
below are measured, not asserted.

The DQN (shared network across all 50 users; Critical Implementation Fixes 5--6
above applied) was trained for 300 episodes. Evaluation reward rose from $-41.2$
(early training, near-random) to a noisy plateau around $-32$ to $-36$ by episode
300, with Jain fairness rising from $0.54$ to $\approx0.6$--$0.68$ and outage
falling from $15.1/50$ to $\approx 8$--$10/50$; the remaining episode-to-episode
noise reflects the randomized initial battery SoC and weather draw per training
episode, not non-convergence of the policy itself.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{./figures/8_dqn_association_convergence.png}
\caption{DQN training convergence on the association MDP: evaluation reward, Jain
fairness, outage, and mean battery SoC vs.\ training episode.}
\label{fig:dqn-association-convergence}
\end{figure}
\FloatBarrier

Table~\ref{tab:phase2-results} extends Section~IV.D's baseline
(Table~\ref{tab:assoc-baseline-results}) with the trained DQN, and additionally
reports the reward $R(\mathbf{a})$ defined above for all three policies, since that
composite metric is first defined in this section.

\begin{table}[t]
\centering
\caption{Phase 2 validation: 50 held-out 24-hour episodes (seed 999), 50 users, mean $\pm$ std}
\label{tab:phase2-results}
{\scriptsize\setlength{\tabcolsep}{3pt}
\begin{tabularx}{\columnwidth}{|>{\raggedright\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|>{\centering\arraybackslash}X|}
\hline
\textbf{Policy} & \textbf{Reward $R(\mathbf{a})$} & \textbf{Jain} & \textbf{Outage /50} & \textbf{Throughput [bits/s/Hz]}\\
\hline
Random & $-82.2\pm17.0$ & $0.331\pm0.046$ & $33.7\pm3.3$ & $33.95\pm4.75$ \\
\hline
Max-SINR (no DRL) & $\mathbf{-19.3\pm17.5}$ & $\mathbf{0.700\pm0.027}$ & $\mathbf{1.0\pm0.9}$ & $\mathbf{76.18\pm3.57}$ \\
\hline
DQN & $-34.1\pm13.8$ & $0.607\pm0.045$ & $9.8\pm3.3$ & $66.99\pm5.39$ \\
\hline
\end{tabularx}}
\end{table}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{./figures/9_baseline_comparison.png}
\caption{Random vs.\ max-SINR vs.\ DQN, 50 held-out episodes (error bars: std).}
\label{fig:baseline-comparison}
\end{figure}
\FloatBarrier

\subsubsection{Discussion: DQN vs.\ Plain Mathematical Evaluation}

Both policies comfortably beat random ($4\times$ better reward, $30\times$ lower
outage). \textbf{The DQN does not yet beat the closed-form max-SINR baseline} on this
MDP configuration -- it sits clearly between random and max-SINR on every metric.
Inspecting the learned policy directly (post Error 5/6 fixes) shows it agrees with
the max-SINR choice for 92\,\% of users; the disagreements are where it does worse,
not better. This is a genuine, measured result, not a training bug: since users and
the constellation are both static for the whole episode, the instantaneous best-SINR
choice is already close to throughput/fairness-optimal for most users most of the
time, and $P_{k,\text{user}}=1$~W is deliberately small relative to the fixed 700~W
baseline draw (Section~\ref{sec:assoc-baseline-setup}) -- so the situations where
trading SINR for battery protection or fairness would actually help are a minority
the DQN, at this training budget (300 episodes, no hyperparameter search), has not
reliably learned to detect. In short: for this exact reward and this exact 3-HAPS
constellation, plain mathematical evaluation (max-SINR) is currently the stronger
policy, and closing that gap -- more training, a reward or exploration schedule that
surfaces the rarer battery/fairness trade-off cases more often, or a harder scenario
mix where max-SINR provably fails -- is future work, not yet a validated Phase~2
result.

\subsubsection{Phase 3 Scope: Not Yet Implemented}

Two pieces of the wider system design are referenced elsewhere in this document but
are \textbf{not implemented} in the Phase~2 results above:
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \textbf{LSTM weather forecaster}: Section~\ref{subsec:scintillation}'s
    tropospheric scintillation fading and the LSTM-based forecast of it from
    rainfall/visibility timeseries (needed for adaptive margin reallocation) are
    deferred to Phase~3. Phase~2 uses only the static weather features
    (rain, visibility, hour) already in the state vector.
  \item \textbf{UAV relay}: the topology's 1 static UAV relay (Table~\ref{tab:config})
    is geometrically present but its user-facing SINR and the two-hop
    User$\to$UAV$\to$HAPS path are unmodeled; the DQN action space here is exactly
    $\{\text{HAPS1},\text{HAPS2},\text{HAPS3}\}$, with $a=3$ (UAV) deferred.
\end{itemize}
Both are required before any Phase~3 claim can be validated the same way
Section~\ref{sec:phase2-results} validates Phase~2.

\section{Abbreviations}

\begin{tabular}{ll}
\textbf{ABS} & Aerial Base Station \\
\textbf{AGL} & Above Ground Level \\
\textbf{CDF} & Cumulative Distribution Function \\
\textbf{DRL} & Deep Reinforcement Learning \\
\textbf{DQN} & Deep Q-Network \\
\textbf{FDMA} & Frequency Division Multiple Access \\
\textbf{FSPL} & Free-Space Path Loss \\
\textbf{GW} & Gateway \\
\textbf{HAPS} & High-Altitude Platform Station \\
\textbf{ITU} & International Telecommunication Union \\
\textbf{K-band} & 18--27 GHz (used as ITU-R P.618 reference for scintillation model) \\
\textbf{Ka-band} & 27--40 GHz (our feeder link 38 GHz; scaled from K-band via $(f/20)^{0.7}$) \\
\textbf{LoS} & Line-of-Sight \\
\textbf{LSTM} & Long Short-Term Memory \\
\textbf{LWC} & Liquid Water Content \\
\textbf{MDP} & Markov Decision Process \\
\textbf{MLP} & Multi-Layer Perceptron \\
\textbf{MVP} & Minimum Viable Product \\
\textbf{NLoS} & Non-Line-of-Sight \\
\textbf{PDF} & Probability Density Function \\
\textbf{PL} & Path Loss \\
\textbf{RH} & Relative Humidity \\
\textbf{SINR} & Signal-to-Interference-plus-Noise Ratio \\
\textbf{SoC} & State of Charge (Battery) \\
\textbf{TDMA} & Time Division Multiple Access \\
\textbf{TD} & Temporal Difference \\
\textbf{UAV} & Unmanned Aerial Vehicle \\
\textbf{UTD} & Uniform Theory of Diffraction \\
\end{tabular}

\section {References}
% =====================================================================
\renewcommand{\refname}{}
\begin{thebibliography}{30}

% === ITU-R Recommendations (Propagation) ===
\bibitem{itu_m2101}
ITU-R Recommendation M.2101-0, ``Characteristics of High Altitude Platform Stations
(HAPS) operating in the stratosphere,'' 2017.
Available: \url{https://www.itu.int/rec/R-REC-M.2101/latest/}

\bibitem{itu_m2106}
ITU-R Recommendation M.2106-1, ``Characteristics of High Altitude Platform Stations
(HAPS) operating in the stratosphere,'' 2012.
Available: \url{https://www.itu.int/rec/R-REC-M.2106/latest/}

\bibitem{itu_p530}
ITU-R Recommendation P.530-17, ``Propagation data and prediction methods required
for the design of terrestrial line-of-sight systems,'' 2021.
Available: \url{https://www.itu.int/rec/R-REC-P.530/latest/}

\bibitem{itu_p618}
ITU-R Recommendation P.618-13, ``Propagation data and prediction methods for the
design of Earth-space telecommunication systems,'' 2021.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.618-13-202108-S!!PDF-E.pdf}
[Covers rain attenuation reduction factors, slant-path geometry, scintillation model]

\bibitem{itu_p838}
ITU-R Recommendation P.838-3, ``Specific attenuation model for rain for use in
prediction methods,'' 2005.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.838-3-200503-S!!PDF-E.pdf}
[Annex 1: frequency-dependent rain coefficients $k(f)$, $\alpha(f)$]

\bibitem{itu_p676}
ITU-R Recommendation P.676-12, ``Attenuation by atmospheric gases and related
effects,'' 2021.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.676-12-202112-S!!PDF-E.pdf}
[Section 3.1.2: O$_2$ resonance (59.8 GHz, 118.75 GHz); Section 3.2.1: H$_2$O resonance (22.235 GHz)]

\bibitem{itu_p840}
ITU-R Recommendation P.840-6, ``Attenuation due to clouds and fog,'' 2021.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.840-6-202101-I!!PDF-E.pdf}
[Liquid water content model for fog/cloud attenuation]

\bibitem{itu_p835}
ITU-R Recommendation P.835-7, ``Reference standard atmospheres,'' 2017.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.835-7-201703-I!!PDF-E.pdf}

\bibitem{itu_p836}
ITU-R Recommendation P.836-5, ``Surface water vapour statistics,'' 2017.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.836-5-201703-I!!PDF-E.pdf}

\bibitem{itu_p837}
ITU-R Recommendation P.837-7, ``Characteristics of precipitation for propagation
modelling,'' 2017.
Available: \url{https://www.itu.int/dms_pubrec/itu-r/rec/p/R-REC-P.837-7-201706-I!!PDF-E.pdf}

\bibitem{itu_p1410}
ITU-R Recommendation P.1410-5, ``Propagation data and prediction methods for the
design of terrestrial broadband radioaccess systems operating in a frequency range
from 3 to 60 GHz,'' 2021.
Available: \url{https://www.itu.int/rec/R-REC-P.1410/latest/}
[Elevation-angle-dependent LoS probability baseline]

% === 3GPP / NTN Standards ===
\bibitem{3gpp_ts38821}
3GPP TS 38.821, ``Study on New Radio (NR) and its Evolution for Non-Terrestrial
Networks (NTN),'' Release 17, September 2022.
Available: \url{https://www.3gpp.org/DynaReport/38821.htm}
[HAPS frequency allocations: 38--39.5 GHz feeder, 2.1--2.2 GHz service; UAV relay scenarios]

\bibitem{3gpp_ts36104}
3GPP TS 36.104, ``Evolved Universal Terrestrial Radio Access (E-UTRA); Base Station
(BS) Radio Transmission and Reception,'' Release 16, December 2021.
Available: \url{https://www.3gpp.org/DynaReport/36104.htm}
[LTE channel bandwidth specifications; 20 MHz service-link allocation]

% === Peer-Reviewed Publications ===
\bibitem{arani2023}
A.~H. Arani, P.~Hu, and Y.~Zhu, ``HAPS-UAV-Enabled Heterogeneous Networks: A Deep
Reinforcement Learning Approach,'' \emph{IEEE Open J. Commun. Soc.}, vol.~4,
pp.~1745--1760, 2023.
DOI: 10.1109/OJCOMS.2023.3294598

\bibitem{holispechac2008}
J.~Holis and P.~Pechac, ``Elevation Dependent Shadowing Model for Mobile
Communications via High Altitude Platforms in Built-up Areas,'' \emph{IEEE
Trans. Antennas Propag.}, vol.~56, no.~4, pp.~1078--1084, Apr. 2008.
DOI: 10.1109/TAP.2007.914935
[Building-blockage LoS probability series; environment parameters (Table I): $(\alpha, \beta, \xi)$ for suburban/urban/dense-urban/high-rise]

\bibitem{bakambekova2024}
A.~Bakambekova, N.~Kouzayha, and T.~Al-Naffouri, ``On the Interplay of Artificial
Intelligence and Space-Air-Ground Integrated Networks: A Survey,'' \emph{IEEE Open
J. Commun. Soc.}, vol.~5, pp.~4613--4673, 2024.

% === Regulatory / Industry Standards ===
\bibitem{ecc_report_156}
European Communications Committee (ECC), ``Minimum performance characteristics
for spectrum sharing between Fixed Satellite Service (FSS) and other services,''
ECC Report 156, April 2011 (updated December 2014).
Available: \url{https://www.ecc.cept.org/ecc/documents/ecc-reports/}
[Spectrum-sharing guidance; rain attenuation margins at Ka-band]

\bibitem{ecc_rec_04_05}
European Communications Committee (ECC), ``Radio Frequency Channel Arrangements for
Fixed Service Systems Operating in the Bands 6--7 GHz and 36--40 GHz,''
ECC Recommendation (04)05, Strasbourg, France.
[Ka-band channelization for backhaul systems]

% === SINR and Signal Analysis ===
\bibitem{itu_p618}
ITU-R, ``Propagation data and prediction methods for the design of Earth-space
telecommunication systems,'' Recommendation ITU-R P.618--13, Geneva, Switzerland, 2017.
[SINR definition $\gamma = C/(N+I)$, rain attenuation, feeder-link margins]

\bibitem{goldsmith2005}
A.~Goldsmith, \emph{Wireless Communications}, Cambridge University Press, 2005.
[Multi-user SINR in linear domain; noise and interference are additive in linear scale, not dB]

\bibitem{3gpp_ntn}
3GPP, ``Study on New Radio (NR) and its evolution for Non-Terrestrial Networks,''
Technical Report 3GPP TR 38.821, Release 16, April 2020.
[Multi-beam HAPS interference model: $\mathrm{SINR}_k = P_{\mathrm{serving}} \cdot G_k / (N_0 + \sum P_i \cdot G_{i,k})$]

% === Implementation Libraries ===
\bibitem{itur_package}
I.~Portillo-Ramirez, ``itur: Python implementation of ITU-R P recommendations,''
GitHub, 2024. Available: \url{https://github.com/iportillo/itur}
[Bundled ITU-R models for rain, fog, gas, scintillation]

% === Deep Reinforcement Learning (Phase 2) ===
\bibitem{mnih2015dqn}
V.~Mnih, K.~Kavukcuoglu D.~Silver, A.~A.~Rusu, J.~Veness, G.~Bellemare,
A.~Graves, M.~Riedmüller, A.~K.~Fidjeland, G.~Ostrovski, S.~Petersen,
C.~Beattie, A.~Sadik, I.~Antonoglou, H.~King, D.~Kumaran, D.~Wierstra,
S.~Legg, and D.~Hassabis, ``Human-level control through deep reinforcement
learning,'' \emph{Nature}, vol.~529, pp.~529--533, 2015.
[Foundational DQN algorithm: experience replay, target networks, Q-learning]

\bibitem{sutton2018rl}
R.~S.~Sutton and A.~G.~Barto, \emph{Reinforcement Learning: An Introduction},
2nd~ed. Cambridge, MA: MIT Press, 2018.
[MDP theory, value functions, policy gradient methods; Chapter 6: Q-learning]

\bibitem{schulman2017ppo}
J.~Schulman, F.~Wolski, P.~Dhariwal, A.~Radford, and O.~Klimov,
``Proximal policy optimization algorithms,'' arXiv preprint arXiv:1707.06347, 2017.
[Policy-gradient alternative to DQN; higher sample complexity for discrete actions]

\bibitem{goldsmith2005wireless}

\bibitem{mnih2015human}
V.~Mnih et~al., ``Human-level control through deep reinforcement learning,''
\textit{Nature}, vol.~529, no.~7587, pp.~529--533, Feb.~2015.

\bibitem{jain1984quantitative}
R.~K.~Jain et~al., ``A quantitative measure of fairness and discrimination for resource allocation in shared computer systems,''
\textit{arXiv preprint arXiv:cs/9809099}, 1984.

\bibitem{sutton2018reinforcement}
R.~S.~Sutton and A.~G.~Barto, \textit{Reinforcement Learning: An Introduction},
2nd~ed. MIT Press, 2018.
A.~Goldsmith, \emph{Wireless Communications}, 2nd~ed. Cambridge University Press, 2005.
[Chapter 3: SINR computation in linear domain, not dB; multi-user interference]

\end{thebibliography}

\newpage

\appendix

\onecolumn

% =====================================================================

\subsection{Core Functions}

The listings below show the core functions realising \eqref{eq:fspl}--\eqref{eq:sinr} and the scintillation fade-depth models.

%\begin{figure}[!htbp]
\begin{lstlisting}[caption={Core channel-model functions},label={lst:core}]
import numpy as np
C = 3e8  # speed of light [m/s]

# (1) HAPS free-space path loss  ->  eq. (1)
def haps_fspl_db(f_mhz, d_km):
    return 32.44 + 20*np.log10(f_mhz) + 20*np.log10(d_km)

# (1b) HAPS-to-user/gateway air-to-ground path loss -> eq. (1), Arani et al. Eq. 2
# Pure free-space path loss: NO landscape/LoS-NLoS term on the HAPS tier
# (service link 2 GHz AND feeder link 38 GHz both use this -- see S:IV.C).
def haps_a2g_pathloss_db(r_horiz, h_haps, f_hz, h_user=1.5):
    dh  = h_haps - h_user
    d3d = np.sqrt(r_horiz**2 + dh**2)
    return 20*np.log10(4*np.pi*f_hz*d3d/C)

# (2) UAV LoS probability: exact building-blockage series -> eq. (2)-(3)
# UAV/relay tier ONLY (300 m altitude) -- building blockage does not apply
# to the HAPS tier above.
def los_probability(r, h_uav, h_user=1.5, alpha=0.3, beta=500.0, xi=15.0):
    dh = h_uav - h_user
    J  = max(int(np.floor((r/1000.0)*np.sqrt(alpha*beta) - 1.0)), -1)
    P  = 1.0
    for n in range(J + 1):
        frac = (n + 0.5) / (J + 1)
        val  = h_uav - frac*dh          # h_u - frac*(h_u-h_k)
        P   *= 1.0 - np.exp(-(val**2) / (2*xi**2))
    return P

# (3) UAV air-to-ground average path loss  ->  eq. (4)-(5)
# UAV/relay tier ONLY -- LoS/NLoS blend via Holis & Pechac (2008).
def uav_a2g_pathloss_db(r, h_uav, f_hz=2e9, h_user=1.5,
                        eta_los=1.0, eta_nlos=20.0, **env):
    dh   = h_uav - h_user
    d3d  = np.sqrt(r**2 + dh**2)
    plos = los_probability(r, h_uav, h_user, **env)
    base = 20*np.log10(4*np.pi*f_hz*d3d/C)
    return plos*(base+eta_los) + (1-plos)*(base+eta_nlos)

# (4) Received power and noise-limited SINR  ->  eq. (6)
def sinr_db(p_tx_dbm, pl_db, noise_dbm=-100.0):
    p_rx = 10**((p_tx_dbm - pl_db)/10.0)
    return 10*np.log10(p_rx / 10**(noise_dbm/10.0))
\end{lstlisting}
%\end{figure}


%\begin{figure}[!htbp]
\begin{lstlisting}[caption={Scintillation fading model (ITU P.618)},label={lst:scint}]
# Scintillation fade depth (deterministic) -> Equation (12) ITU P.618
def scintillation_fade_depth_db(p_time_percent, elevation_deg, freq_ghz=20.0):
    """ITU P.618 fade depth: D = D_ref * (f/f_ref)^0.7
                                 * (1/sin(elev)) * sqrt(log(1/p))"""
    elev = np.clip(elevation_deg, 5.0, 90.0)
    freq_factor = (freq_ghz / 20.0) ** 0.7
    elev_rad = np.radians(elev)
    elev_factor = 1.0 / np.sin(elev_rad)
    p_frac = p_time_percent / 100.0
    time_factor = np.sqrt(np.abs(np.log(p_frac + 1e-6)))
    ref_fade_db = 2.5  # at p=1%, elev=30deg, f=20GHz
    return ref_fade_db * freq_factor * elev_factor * time_factor

# Random fading envelope (log-normal or Gamma)
def scintillation_fading_envelope(time_samples, p_time_percent, elevation_deg,
                                   freq_ghz=20.0, dist_km=50.0,
                                   distribution='log_normal'):
    """Generate fading multipliers (0-1 linear) matching ITU statistics."""
    fade_depth_db = scintillation_fade_depth_db(p_time_percent, elevation_deg,
                                                freq_ghz)
    fade_depth_linear = 10.0 ** (-fade_depth_db / 20.0)
    p_in_fade = p_time_percent / 100.0
    path_variance_scale = np.sqrt(dist_km / 50.0)

    if distribution == 'log_normal':
        mu = 0.0
        sigma = np.sqrt(2.0 * np.log(1.0 / fade_depth_linear)) * \
                path_variance_scale
        fading_db = np.random.normal(mu, sigma, time_samples)
        fading_linear = 10.0 ** (fading_db / 20.0)
    else:  # Gamma
        shape = 2.0 * path_variance_scale
        scale = 1.0 / (shape * (1.0 - p_in_fade))
        fading_linear = np.random.gamma(shape, scale, time_samples)

    return np.clip(fading_linear, fade_depth_linear, 1.0)

# Total feeder link loss including scintillation
def feeder_link_loss_with_scintillation(distance_km, elevation_deg,
    freq_ghz=20.0, p_time_percent=1.0, include_scint=True):
    """L_total = L_FSPL + D(p,theta,f) -> eq. (14)"""
    fspl_db_val = 20*np.log10(4*np.pi*freq_ghz*1e9*distance_km*1000 / 3e8)
    if include_scint:
        fade_depth_db = scintillation_fade_depth_db(p_time_percent,
                                                    elevation_deg, freq_ghz)
        return fspl_db_val + fade_depth_db
    else:
        return fspl_db_val
\end{lstlisting}
%\end{figure}

%\begin{figure}[!htbp]
\begin{lstlisting}[language=Python,label={lst:atm_api},caption={Atmospheric losses API (atmospheric\_losses.py)}]
# Import functions
from atmospheric_losses import (
    rain_attenuation_db,        # ITU-R P.838 rain model
    fog_attenuation_db,         # ITU-R P.840 fog model
    gas_attenuation_db,         # ITU-R P.676 gas absorption
    scintillation_fading_db,    # ITU-R P.618 scintillation
    total_feeder_loss_db,       # Combined sum of all effects
    get_rainfall_rate_for_availability,  # P.837 climatology
    get_standard_atmosphere,    # Reference atmosphere
)

# Example: Link budget at 38 GHz, 11.3$^\circ$ elevation
losses = total_feeder_loss_db(
    rain_rate_mmhr=10.0,        # Design rain rate [mm/h]
    liquid_water_g_m3=0.05,     # Fog density [g/m^3]
    pressure_hpa=1013.0,        # Barometric pressure
    temp_c=15.0,                # Temperature
    rh_percent=60.0,            # Relative humidity
    freq_ghz=38.0,              # Frequency
    elevation_deg=45.0,         # Elevation angle
    time_percentage=0.01,       # Outage allowance [%]
    lat=28.5, lon=77.2          # Location (Delhi)
)

print(f"Total atmospheric loss: {losses['total']:.2f} dB")
print(f"  Rain:     {losses['rain']:.2f} dB")
print(f"  Fog:      {losses['fog']:.2f} dB")
print(f"  Gas:      {losses['gas']:.2f} dB")
print(f"  Scint:    {losses['scintillation']:.2f} dB")
\end{lstlisting}
%\end{figure}

%\begin{figure}[!htbp]
\begin{lstlisting}[language=Python,label={lst:climate_api},caption={Climate data API}]
from atmospheric_losses import (
    get_rainfall_rate_for_availability,
    get_rainfall_probability,
)

# Get rain rate for 99.99% link availability (0.01% outage)
# This is the "1-in-10000" rain event from 30-year climatology
rain_99_99pct = get_rainfall_rate_for_availability(
    lat=28.5, lon=77.2,
    availability_percent=99.99
)

# Get probability of rain occurrence
prob_rain = get_rainfall_probability(lat=28.5, lon=77.2)

print(f"Design rain rate: {rain_99_99pct:.1f} mm/h (0.01% exceedance)")
print(f"Rain probability: {prob_rain:.1f}% of average year")
\end{lstlisting}
%\end{figure}


\subsection{Source Model Parameters (Arani et al.\ Table 1)}
\label{sec:app-arani-table1}

Table~\ref{table:app-arani-table1} reproduces the system-level simulation parameters 
from~\cite{arani2023} (Table 1, p.\ 1755) -- the primary source cited throughout this 
document's channel model. It is included here as a reference for which constants
in \texttt{channel\_model.py} are adopted directly, and which are intentionally 
not (this project uses its own carrier-frequency plan; see \S III.A).

\begin{table}[htbp]
\centering
\caption{System-level simulation parameters from Arani, Hu \& Zhu (2023), Table 1, and their adoption status in this project's channel model.}
\label{table:app-arani-table1}
\small
\begin{tabular}{lll}
\hline
\textbf{Parameter} & \textbf{Value (Arani et al.)} & \textbf{Used here?} \\
\hline
HAPS altitude & 20 km & Yes -- matches \texttt{HAPS\_ALT\_M} \\
UAV altitude range ($h_{\min}$, $h_{\max}$) & 22.5\,m, 150\,m & No -- plots use 300\,m \\
User height & 1.5 m & Yes \\
Carrier freq.\ (UAV, HAPS) & 28 GHz, 2.11 GHz & No -- this project uses its own 2\,GHz service/relay and $\sim$38\,GHz feeder plan (\S III.A), not Arani's 28\,GHz UAV band \\
Transmit power (HAPS, UAV) & 43 dBm, 24 dBm & Yes -- \texttt{P\_TX\_HAPS\_DBM}, \texttt{P\_TX\_UAV\_DBM} \\
Noise power spectral density & $-174$ dBm/Hz & No -- flat \texttt{NOISE\_DBM}=$-100$\,dBm used instead (consistent with $\sim$14\,MHz channel BW) \\
$\alpha,\beta,\xi$ (LoS probability) & 0.1, 750, 8 & Yes -- matches this document's ``suburban'' preset exactly (\S III.C); Arani et al.\ simulated only this one environment, sourced from Holis \& Pechac (2008) Table I \\
Reference path loss $\delta$ & 61.4 dB & No (Phase 2) -- calibrated to Arani's 28\,GHz; would need recomputing at this project's 2\,GHz ($\approx$38.5\,dB via free-space path loss at 1\,m) \\
Path-loss exponent $\eta$ (LoS/NLoS) & 2, 3 & No (Phase 2) -- not yet adopted; current \texttt{eta\_los}/\texttt{eta\_nlos} remain flat dB offsets \\
Shadowing std.\ dev.\ $\chi$ (LoS/NLoS) & 5.8, 8.7 dB & No (Phase 2) -- shadow fading not currently modeled (deterministic mean-value path loss only) \\
\hline
\end{tabular}
\end{table}

\FloatBarrier

\subsection{Data Analysis Tables}
\label{sec:app-data-tables}

The following tables present comprehensive multi-distance and multi-frequency path loss and SINR analysis derived from the channel model using linear-domain calculations per Equations~\eqref{eq:sinr-multinode} and \eqref{eq:pathlos-blend}.
\begin{table}[htbp]
\centering
\caption{Table A.2: Multi-Distance SINR (2\,GHz service link, +X axis from nadir, corrected 65\,km equilateral 3-HAPS constellation, $P_{\mathrm{TX,HAPS}}=43$\,dBm). Four distances (0--100\,km), SINR degradation from $+7.3\,\mathrm{dB}$ (nadir) to $-9.1\,\mathrm{dB}$ (100\,km edge). Landscape-independent (pure free-space path loss, \S III.C). Demonstrates DRL scheduling necessity. Related figure: Figure~\ref{fig:multinode-interference}. Linear-domain calculation per Equation~\eqref{eq:sinr-multinode}.}
\label{table:app-sinr-multinode-distances}
\small
\input{./appendix_tables/Path_loss_comparison_2ghz_38ghz.tex}
\caption{Table A.1: Path Loss Comparison (2 GHz service link, user at 1.5\,m vs.\ 38 GHz feeder link, gateway at 50\,m AGL). Eight distances (0--150\,km) from HAPS nadir. Pure free-space path loss per Eq.~\eqref{eq:fspl} -- the HAPS-side links have no landscape-dependent excess loss (\S III.C; Arani et al.\ Eq.\ 2). Frequency offset $20\log_{10}(38/2) \approx 25.58$~dB is independent of distance (constant at all distances), derived by canceling distance terms in Eq.~\eqref{eq:fspl}. Plotted version: Figure~\ref{fig:fspl_comparison}. Full data in \texttt{appendix\_data/} CSV files.}
\label{table:app-pathloss-2ghz-38ghz}
\end{table}
\begin{table}[htbp]
\centering
\small
\input{./appendix_tables/SINR_multinode_table_distances.tex}
\end{table}

\subsection{Table A.3: Spatial Heterogeneity Analysis (2D SINR Distribution)}
\label{sec:app-spatial-sinr}

The following analysis extends the multi-distance SINR model to include spatial heterogeneity across the triangular constellation geometry. Users at the same distance from the serving HAPS experience dramatically different SINR values depending on their angular position relative to the interferers, violating the distance-only assumption of classical path-loss models. See Figure~\ref{fig:app-constellation-sectors} (\S\ref{subsec:multinode-sinr}) for the constellation geometry and resulting good/bad sectors.
%\begin{minipage}[t]{0.48\textwidth}
\begin{table}[h!]
\centering
\footnotesize
\caption{Table A.3a: SINR at $\theta$~=~0$^\circ$ (Worst-Case: Toward I1)}
\label{table:app-sinr-0deg}
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{r (km)} & \textbf{$d_S$} & \textbf{$d_{I1}$} & \textbf{$d_{I2}$} & \textbf{SINR (dB)} \\
\hline
10 & 10.0 & 55.0 & 60.6 & 5.43 \\
30 & 30.0 & 35.0 & 56.3 & --0.83 \\
50 & 50.0 & 15.0 & 58.9 & --7.40 \\
75 & 75.0 & 10.0 & 70.5 & --11.27 \\
100 & 100.0 & 35.0 & 87.9 & --9.06 \\
\hline
\end{tabular}
\end{table}
%\end{minipage}

%\begin{minipage}[t]{0.48\textwidth}
\begin{table}[h!]
\centering
\footnotesize
\caption{Table A.3b: SINR at $\theta$~=~90$^\circ$ (Perpendicular)}
\label{table:app-sinr-90deg}
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{r (km)} & \textbf{$d_S$} & \textbf{$d_{I1}$} & \textbf{$d_{I2}$} & \textbf{SINR (dB)} \\
\hline
10 & 10.0 & 65.8 & 56.6 & 5.81 \\
30 & 30.0 & 71.6 & 41.8 & 0.52 \\
50 & 50.0 & 82.0 & 33.1 & --3.89 \\
75 & 75.0 & 99.2 & 37.5 & --6.17 \\
100 & 100.0 & 119.3 & 54.5 & --6.20 \\
\hline
\end{tabular}
\end{table}
%\end{minipage}


\noindent
\begin{minipage}[t]{0.48\textwidth}
\centering
\footnotesize
\captionof{table}{Table A.3c: SINR at $\theta=180^\circ$ (Away from Constellation)}
\label{table:app-sinr-180deg}
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{r (km)} & \textbf{$d_S$} & \textbf{$d_{I1}$} & \textbf{$d_{I2}$} & \textbf{SINR} \\
\hline
10 & 10.0 & 75.0 & 70.5 & 7.13 \\
30 & 30.0 & 95.0 & 84.1 & 4.47 \\
50 & 50.0 & 115.0 & 99.9 & 2.26 \\
75 & 75.0 & 140.0 & 121.3 & 0.40 \\
100 & 100.0 & 165.0 & 144.0 & --0.92 \\
\hline
\end{tabular}
\end{minipage}
\hfill
\begin{minipage}[t]{0.48\textwidth}
\centering
\footnotesize
\captionof{table}{Table A.3d: SINR at $\theta=270^\circ$ (Opposite Perp.)}
\label{table:app-sinr-270deg}
\begin{tabular}{|c|c|c|c|c|}
\hline
\textbf{r (km)} & \textbf{$d_S$} & \textbf{$d_{I1}$} & \textbf{$d_{I2}$} & \textbf{SINR} \\
\hline
10 & 10.0 & 65.8 & 73.8 & 6.80 \\
30 & 30.0 & 71.6 & 92.2 & 3.70 \\
50 & 50.0 & 82.0 & 111.1 & 1.33 \\
75 & 75.0 & 99.2 & 135.3 & --0.50 \\
100 & 100.0 & 119.3 & 159.6 & --1.70 \\
\hline
\end{tabular}
\end{minipage}

% =====================================================================
\subsection{Appendix A.4: System Topology and Multi-HAPS Interference Design Space}
\label{sec:app-topology-tradeoffs}
% =====================================================================

\subsubsection{Topology-Specific Nature of Multi-HAPS Interference}

The 10--20\,dB multi-HAPS interference penalty documented in \S IV.B is \textbf{not an inherent property of multi-platform HAPS systems}, but rather a \textbf{consequence of the specific topology chosen for this project}. This section clarifies the design space and topology trade-offs that led to the current 3-HAPS constellation architecture.

\paragraph{Current topology (3-HAPS constellation, equilateral triangle):}
\begin{itemize}\setlength{\itemsep}{0pt}
  \item \textbf{Service links (2~GHz)}: Direct HAPS-to-user links from multiple HAPS in constellation → \textbf{interference-limited} (10--20\,dB penalty), requires DQN scheduling
  \item \textbf{Feeder links (38~GHz)}: HAPS-to-gateway links at short range (10--500\,m) → \textbf{noise-limited} (negligible interference, gateways 65~km from interferers)
  \item \textbf{Relay links (2~GHz)}: UAV-to-user fallback at 300\,m altitude → secondary, landscape-dependent coverage
  \item \textbf{Design trade-off}: Multiple HAPS provide 100~km coverage but introduce service-link interference; DQN-based load balancing mitigates this penalty
\end{itemize}

\paragraph{Alternative topologies (not pursued here):}

\begin{enumerate}
  \item \textbf{Single-HAPS system}: No multi-HAPS interference by definition. Coverage limited to ~30~km radius around nadir (path-loss-limited beyond). Would require multiple gateways and extensive relay network to serve 100~km footprint; this defeats the purpose of high-altitude platforms.

  \item \textbf{Relay-only system} (no direct HAPS service links): UAVs at 300\,m provide all user connectivity; HAPS only transmits on 38~GHz feeder to relay-aggregation gateways. Multi-HAPS constellation no longer creates interference (different altitudes, separated by up to 20~km). Trade-off: relay density requirements, relay power consumption, and latency increase significantly; not practical for continuous coverage.

  \item \textbf{Frequency-separated multi-HAPS}: Each HAPS transmits on distinct 2~GHz sub-bands (e.g., HAPS1 on 2.0--2.05~GHz, HAPS2 on 2.05--2.1~GHz, HAPS3 on 2.1--2.15~GHz). Eliminates interference but halves effective bandwidth per HAPS and requires sophisticated filter hardware; regulatory approval for this fragmentation is unclear in ITU-R M.2101.

  \item \textbf{Time-division multi-HAPS}: Only one HAPS transmits at a time on 2~GHz; others listen on feeder for coordination. Zero interference but throughput reduced by factor of 3. Latency and coordination overhead become prohibitive.
\end{enumerate}

\paragraph{Why the current 3-HAPS constellation was chosen:}

The 3-HAPS equilateral-triangle constellation balances four competing objectives:
\begin{enumerate}
  \item \textbf{Coverage continuity}: Three HAPS separated by 65~km overlap sufficiently to cover 100~km footprint with >85$^\circ$ elevation angles (high availability).
  \item \textbf{Interference tolerance}: 10--20\,dB penalty on 2~GHz service links is manageable with DQN scheduling; Phase~1 measurements confirm average SINR $\approx +2$~dB even with interference, supporting real-time scheduling overhead.
  \item \textbf{Gateway simplicity}: Single high-performance gateway per HAPS suffices (short 10--500~m feeder links, high-gain antennas feasible at 38~GHz, noise-limited operation).
  \item \textbf{Relay integration}: UAV relays serve only marginal users (far from HAPS nadir or in deep shadow); computational burden on DQN remains tractable (3 HAPS $\times$ 2~GHz + 3 relays $\times$ 2~GHz $\approx$ 6 interference scenarios to model, vs. 10$+$ in denser networks).
\end{enumerate}

\paragraph{Implications for future work:}

If a future project shifts to a relay-dominant architecture (e.g., many low-altitude aerial platforms with limited HAPS), multi-HAPS interference would become negligible and the DQN reward function would simplify substantially. Conversely, if dense HAPS deployments (>5 nodes) become feasible, interference mitigation strategies beyond DQN scheduling (e.g., power control, subcarrier allocation, or cognitive radio techniques) would be necessary, pushing the system toward the complexity of terrestrial 5G networks.

\end{document}
